---
baseline_commit: 10c6df882cf2ba494b364397c87d85b1edd4272d
---

# Story 1.5: Super Admin — Create & Onboard a Gym

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As GymOS platform staff (Super Admin),
I want to create a new gym record and owner account,
so that a new customer gym can be onboarded and its owner can access the platform.

## Acceptance Criteria

1. **Given** the Super Admin Gyms page, **When** I fill in gym name, owner name, owner phone, and subscription tier, then submit, **Then** a gym record and owner account are created, the gym appears in the Gym List, and the owner receives an SMS with login instructions. [Source: epics.md#Story 1.5]
2. **Given** a gym name that already exists on the platform, **When** I submit the Create Gym form, **Then** I see an inline error, **And** no record is created. [Source: epics.md#Story 1.5]
3. **Given** a newly created gym set to Active status, **When** the owner logs in for the first time, **Then** they land on their own gym's dashboard with no visibility into any other tenant. [Source: epics.md#Story 1.5]

## Tasks / Subtasks

- [x] **Task 1: Migration `0010_super_admin_gym_provisioning.sql`** (AC: #1, #2)
  - [x] `private.is_super_admin()` — new `STABLE` helper, same shape/never-raises discipline as `private.gym_id()` (0009): `returns coalesce((auth.jwt() ->> 'app_role') = 'super_admin', false)`. Replaces the inline `(auth.jwt() ->> 'app_role') = 'super_admin'` check that `log_audit_event()` (0007) already duplicates once — reuse it there too if convenient, but do not modify 0007 (already shipped/committed; touching a landed migration is out of bounds — add a comment there at most).
  - [x] `create unique index idx_gyms_name_unique on gyms (lower(name));` — AC #2 needs a real DB-level uniqueness guarantee (case-insensitive: "FitZone Yaoundé" and "fitzone yaoundé" must collide), not just an app-side pre-check, to close the race-condition window between two concurrent Super Admin sessions. Cannot be added to `0002_gyms_and_tiers.sql` (already shipped) — add here instead.
  - [x] RLS policies (explicit per-action, never `FOR ALL`, per architecture's RLS strategy):
    - `gyms`: `SELECT` and `INSERT` for `private.is_super_admin()`. Do **not** add `UPDATE` — suspend/deactivate/tier-change is Story 1.6's job (matches the established "don't add the next story's policy early" discipline from Stories 1.3/1.4's Scope Boundary notes).
    - `tiers`: `SELECT` only for `private.is_super_admin()` — needed so the Create Gym form's tier dropdown can list current tiers. Tier CRUD (INSERT/UPDATE/DELETE) is Story 1.6's job; do not add those policies here.
    - `members`: `INSERT` only for `private.is_super_admin()`, with `with check (role = 'owner')` — a Super Admin creating a gym may only ever insert the owner row through this path, never an arbitrary role. **Refined during implementation:** also added `SELECT` scoped to `role = 'owner'` only (`using (private.is_super_admin() and role = 'owner')`) — SA-02/SA-03 both display "Owner: <name> (<phone>)," which is unreachable without it. This is deliberately narrower than a general members-SELECT policy: only the one owner row per gym is visible, never coach/receptionist/member rows, keeping it distinct from Story 1.7's audit-logged escalation (FR-072) to a gym's full member/payment data.
    - No new policy needed on `users` — `handle_new_user()` (0003) is already `SECURITY DEFINER` and creates the owner's `public.users` row automatically when `auth.users` gets the new row; the calling role's own grants are irrelevant to it.
  - [x] Seed the three default tiers (FR-073), idempotently (`insert ... where not exists (select 1 from tiers)` guard, or a stable well-known UUID with `on conflict do nothing`), using SA-06's mockup as the only source of concrete numbers (`ux-gym_os-2026-07-04/EXPERIENCE.md` line ~1489-1500): Hustle (`member_cap` 30, monthly 15000, annual 150000), Grind (`member_cap` 100, monthly 35000, annual 350000), Elite (monthly 75000, annual 750000). **Elite's "no cap" cannot be represented** — `tiers.member_cap` is `integer not null` with no sentinel convention yet. Use a large sentinel (`1000000`) with an inline comment explaining it's provisional. See Dev Notes → Open Question 1 before finalizing this value; do not treat it as a real schema decision without confirming.
  - [x] Record all of the above (helper, unique index, RLS scope, seed data + Elite sentinel) in `docs/decisions.md`, matching the established dated-entry format. (Covered by Task 8's consolidated 6-decision entry.)

- [x] **Task 2: `packages/types` gains its first runtime code** (AC: #1, #2)
  - [x] Add `zod` as a dependency of `packages/types` (not previously installed anywhere in the repo — verify the current resolvable version rather than assuming). Installed `^4.4.3` (current stable, confirmed via `npm view zod version`).
  - [x] `packages/types/src/schemas/gym.ts` — `createGymSchema` (Zod): `gymName` (string, min 1), `ownerName` (string, min 1), `ownerPhone` (E.164 string), `ownerEmail` (email string — see Dev Notes → Open Question 2 for why this field exists beyond SA-04's literal mockup), `tierId` (uuid), `status` (one of `gym_status` enum values, default `'active'`). This is the single source of truth for both client-side (submit-time) and server-side (Server Action, never trusts client input) validation, per architecture's Process Patterns.
  - [x] `packages/types/src/errors.ts` — `mapSupabaseError(error)`: maps the unique-violation on `idx_gyms_name_unique` to `{ code: 'gym_name_taken', message: 'A gym with this name already exists' }`, a duplicate-email error from `auth.admin.createUser` to `{ code: 'owner_email_taken', message: '...' }`, and a generic fallback to `{ code: 'unknown', message: 'Something went wrong on our end.' }` (exact copy per EXPERIENCE.md's Error States table). English-only for now — i18n wiring (FR-016's CI gate) is explicitly Story 1.10's job; do not attempt to wire i18n keys prematurely in this story.
  - [x] Export both from `packages/types/src/index.ts` (currently just `export {};` — this is the first story to add real exports).
  - [x] Wire `transpilePackages: ['@gymos/types']` (or the actual workspace package name in `packages/types/package.json`) into `apps/super-admin/next.config.ts`. This was explicitly flagged as deferred in Story 1.1's code review ("currently harmless since the package only exports `export {}`... will matter the moment it gains runtime code") — this story is that moment. `apps/dashboard` doesn't consume `packages/types` yet in this story; wire it there only if the build actually breaks without it (harmless either way, but don't scope-creep unrelated dashboard changes).

- [x] **Task 3: Service-role admin client (super-admin app only)** (AC: #1)
  - [x] `apps/super-admin/lib/supabase/admin.ts` — `createAdminClient()`, a server-only Supabase client constructed with the **service-role key**, distinct from `lib/supabase/server.ts`'s cookie-session client. This is the only file in this story with the service-role key in scope: it exists solely to call `supabase.auth.admin.*` (Admin API), which requires the service-role key and has no RLS-policy equivalent — creating an `auth.users` row cannot be done through the regular session client no matter what RLS policies exist. Add a top-of-file comment stating it must never be imported from a Client Component or any code path reachable by the browser. (Deliberately no `server-only` package added — not in the story spec; the comment satisfies the stated requirement without a new dependency.)
  - [x] Add `SUPABASE_SERVICE_ROLE_KEY` to `apps/super-admin/.env.example` and `.env.local` — **not** `NEXT_PUBLIC_`-prefixed (would leak it to the browser bundle). Do not add it to `apps/dashboard` or `apps/mobile` — this story's need for it is scoped to the Super Admin app's owner-provisioning flow only.
  - [x] Every other read/write in this story (gym insert, tier select, members insert) goes through the **regular session client** (`lib/supabase/server.ts`), governed by the new RLS policies from Task 1 — not the admin client. Reserving service-role usage to exactly the one call that structurally requires it (matches architecture's "Edge Functions reserved for exactly two integrations" minimal-footprint philosophy, applied here to service-role client usage instead).

- [x] **Task 4: Role-gated `(admin)` route group** (AC: #3, and a prerequisite for #1)
  - [x] `apps/super-admin/app/(admin)/layout.tsx` — reads `auth.getClaims()` (same call already used in `app/protected/page.tsx`), redirects to `/auth/login` if unauthenticated OR if `claims.app_role !== 'super_admin'`. **This is a hard security requirement, not a nice-to-have**: today, `apps/super-admin`'s only protected route (`/protected`) accepts *any* authenticated Supabase user — including a gym member or owner from `apps/dashboard`'s auth flow, since both apps share one Supabase project. Without this check, any authenticated user could reach the Super Admin's Gyms page. Leave the existing `/protected` starter route alone (out of scope, no role check needed there — just don't build real Super Admin functionality under it).
  - [x] Minimal shell only — a heading/nav placeholder linking to `/gyms` (the only real page this story builds). Do not build the full sidebar/Metrics/Tiers nav structure; those pages don't exist until Story 1.6.

- [x] **Task 5: SA-02 Gym List page** (AC: #1)
  - [x] `apps/super-admin/services/gyms.ts` — thin service-layer functions wrapping `supabase-js`, per architecture's per-domain service layer pattern (no repository abstraction). **Split from the plan's literal wording during implementation**: `listGyms()`/`listTiers()` live here, but the `createGym` *orchestration* (validation, sequencing, admin-client calls, audit log, SMS) moved to `app/(admin)/gyms/actions.ts` as pure business logic per architecture's boundary ("services/<domain>.ts... no repository abstraction" vs. "business-logic operations go through Server Actions"); `services/gyms.ts` instead exposes thin CRUD primitives (`gymNameExists`, `insertGym`, `deleteGym`, `insertOwnerMember`, `logGymCreated`) that the Server Action composes.
  - [x] `apps/super-admin/app/(admin)/gyms/page.tsx` — Server Component. Query `gyms` joined to `tiers.name` (now visible via the new SELECT policies). Render per SA-02's layout: Gym Name, Owner (name + phone — read from the gym's owner `members` row), Created, Tier, Status. Members column omitted for V1 (no member-count infrastructure needed elsewhere yet).
  - [x] Empty state exact copy (UX-DR10): "No gyms on the platform yet. Create the first one." + `[Create Gym]` button. Loading skeleton: 5 rows (EXPERIENCE.md Loading States table).
  - [x] `[+ Create Gym]` button opens the SA-04 modal (Task 6).
  - [x] **Real bug found and fixed via `next build`/`next dev`, not static review**: this app's `next.config.ts` has pre-existing `cacheComponents: true` (Next.js 16's Cache Components/PPR flag, already present in the starter scaffold, untouched until this story's page was the first to actually exercise dynamic cookie-based data fetching). The route-level `loading.tsx` convention alone was **not** sufficient — Next.js errored the whole route ("Uncached data ... accessed outside of Suspense"), which my own `{data,error}` handling silently caught as a generic query error. Fixed by explicitly wrapping the data-fetching component in `<Suspense>` inside `page.tsx` itself (see File List). Confirmed via a real HTTP request against the running dev server, not just `tsc`.

- [x] **Task 6: SA-04 Create Gym modal + `createGym` Server Action** (AC: #1, #2, #3)
  - [x] `apps/super-admin/app/(admin)/gyms/components/CreateGymModal.tsx` — modal, max-width 480px per SA-04. Fields: Gym Name*, Owner Name*, Owner Phone* (E.164), **Owner Email*** (added — see Dev Notes → Open Question 2), Subscription Tier* (dropdown from `listGyms`'s sibling tier list), Status (defaults Active). Validate on submit only (UX-DR11), using `createGymSchema` client-side for immediate feedback; inline field errors per SA-04's validation table plus the added email field's own required/format error. **Implementation note**: uses the native `<dialog>` element (`showModal()`) rather than a Radix/shadcn Dialog primitive — gets focus-trapping, Escape-to-close, and backdrop for free (UX-DR12) with zero new dependencies, since no Dialog/Select/Toast primitive existed in `components/ui/` yet and none were in this story's approved dependency list.
  - [x] `apps/super-admin/app/(admin)/gyms/actions.ts` — `createGym` Server Action. Never throws for expected errors; returns `{ data, error }`. Implemented the full 10-step sequence exactly as specified (validate → pre-check name → insert gym → admin.createUser → compensating cleanup on failure → generateLink (recovery) → insert owner member → two-deep compensating cleanup on failure → SMS stub → log_audit_event → success). Admin API method names (`auth.admin.createUser`, `auth.admin.generateLink`, `auth.admin.deleteUser`) verified against the installed `@supabase/supabase-js@2.110.0`'s actual types, not assumed.
  - [x] AC #2's inline error: gym name collision surfaces as a per-field error on the Gym Name input, not a toast or page-level error.
  - [x] **Real bug found via manual end-to-end verification, not static review**: `insertOwnerMember` (service layer) initially chained `.select()` after the `members` insert to return the inserted row — but Super Admin's `members` SELECT policy is scoped to `role = 'owner'` rows, and even then, RLS is evaluated on the read-back exactly like any other SELECT. Verified empirically (ad hoc psql session) that the INSERT itself succeeds under the WITH CHECK clause independent of any SELECT policy, but a chained `.select()` is unnecessary complexity given the row's `id` is generated client-side (`crypto.randomUUID()` in the original draft; simplified to let Postgres's own `gen_random_uuid()` default assign it, since the Server Action already has `authUser.user.id` from Step 4 and doesn't need the members row's own `id` back). No chained `.select()` is used on the `members` insert.
  - [x] **Real bug found via manual end-to-end verification**: the SA-04 mockup collects only Gym Name/Owner Name/Owner Phone/Tier/Status, but AC #3 requires the owner to actually be able to log in afterward — verified end-to-end (see Debug Log References) that Owner Email + `auth.admin.createUser({ email, phone, ... })` + a generated recovery link is what makes a real login work, confirming Open Question 2's recommended default was necessary, not just theoretically motivated.

- [x] **Task 7: pgTAP tests** (AC: #1, #2, #3)
  - [x] New file `supabase/tests/gyms_super_admin_rls.test.sql` (18 assertions):
    - `private.is_super_admin()` true/false/absent-claim, mirroring `private.gym_id()`'s coverage.
    - `super_admin` session: sees every gym across tenants, can INSERT a gym, can SELECT tiers, can INSERT a `members` row with `role = 'owner'`, **cannot** INSERT with any other role (WITH CHECK rejects it).
    - Regression: `owner`-claim session still sees exactly their own gym (canary), 0 rows on `tiers`, 0 rows on `members` (unaffected by the new super_admin-scoped policies), and still cannot INSERT a `members` row even with `role='owner'` (WITH CHECK requires `is_super_admin()` too, not just the role value).
    - `idx_gyms_name_unique`: case-insensitive collision rejected.
  - [x] Ran `supabase test db` against local Docker Postgres (WSL2). **All 74 assertions pass** (56 pre-existing + 18 new) — see Debug Log References for two real test-authoring bugs the run itself caught (wrong expected tier count — forgot the migration's own 3-tier seed data; wrong expected members count — forgot the pre-seeded Gym A owner is also visible under the role=owner-scoped policy, not just rows the test session itself inserted).
  - [x] Manual end-to-end verification performed (not skipped): created a gym through the actual code path (real login, real RLS, real `auth.admin.createUser`, real `members` insert, real `log_audit_event()` call) against local Supabase; confirmed the `gyms`/`auth.users`/`public.users`/`members`/`audit_log` rows all exist correctly. Confirmed AC #3 by signing in as the newly created owner via a real recovery-link verification (mirroring what clicking the emailed link does) and confirming `auth.getClaims()`-equivalent access shows `gym_id` = the new gym and a direct `gyms` SELECT under that session returns exactly one row — their own, no other tenant. Also exercised the negative/failure paths: duplicate gym name rejected (unique index), and the two-deep compensating-cleanup path (forced a non-owner `members` insert to fail, confirmed the RLS rejection). See Debug Log References for full detail and the real `cacheComponents`/Suspense bug this pass caught.

- [x] **Task 8: Record deviations in `docs/decisions.md`** (housekeeping — matches Stories 1.2/1.3/1.4's established pattern)
  - [x] One dated entry (6 decisions) covering: `private.is_super_admin()` helper; the `lower(gyms.name)` unique index; the RLS scope decisions including the mid-implementation members-SELECT refinement (role='owner' only, distinct from Story 1.7's escalation); the default-tier seed data and Elite's sentinel cap (Open Question 1); the Owner Email field addition and dual email+phone identity (Open Question 2, confirmed necessary via E2E testing); the SMS-invite stub-provider decision (Open Question 3).
  - [x] Regenerated `packages/types/src/database.ts` via `supabase gen types typescript --local` against local Docker Postgres. **Diff is empty / byte-identical** — this story added RLS policies, an index, and seed data, none of which change any table's column shape, so no update to the generated file was needed or made.

### Review Findings

- [x] [Review][Decision→Patch] The "Gym created. SMS sent to [number]" toast is shown unconditionally even though SMS delivery is a stub (`console.info` only, no real telecom call). The story's own Open Question 3 explicitly flagged this exact tradeoff as needing confirmation before finalizing — no sign-off was recorded before implementation proceeded with the recommended default. [apps/super-admin/app/(admin)/gyms/components/GymsPageClient.tsx:23, apps/super-admin/app/(admin)/gyms/actions.ts:129-134] Sources: Blind Hunter, Acceptance Auditor. **Resolved: keep the stub, make the toast copy honest.** **Fixed**: `createGym` now returns `smsSent: boolean` (always `false` until Story 2.1); `GymsPageClient` shows "Gym created. Invite link generated for [number] — SMS delivery isn't connected yet." when `smsSent` is false.
- [x] [Review][Patch] No `DELETE` RLS policy exists on `gyms` — `deleteGym()`'s compensating cleanup (called on every failure after the gym insert: owner-email-taken, `generateLink` failure, member-insert failure) silently matches 0 rows and never actually removes the row, permanently orphaning an ownerless "active" gym that occupies its name via the unique index. [supabase/migrations/0010_super_admin_gym_provisioning.sql:42-77, apps/super-admin/services/gyms.ts:115-118] Sources: Blind Hunter, Edge Case Hunter, Acceptance Auditor (all three layers independently). **Fixed**: added `super_admin_delete_orphaned_gyms` policy, deliberately scoped to gyms with **no members at all** (`not exists (select 1 from members m where m.gym_id = gyms.id)`) — not a general delete grant; a gym with a real owner must go through Story 1.6's suspend/deactivate lifecycle instead. `deleteGym()` now chains `.select()` and logs if the delete didn't actually match a row. Added 4 new pgTAP assertions (78 total) proving both the positive case (orphaned gym deletable) and negative case (gym-with-owner not deletable, 0 rows affected silently). Re-verified manually end-to-end: forced the member-insert failure path and confirmed both the gym and the auth user are now genuinely gone afterward (previously verified they were NOT).
- [x] [Review][Patch] `admin.auth.admin.deleteUser()` calls in the compensating-cleanup paths don't check their own error — if the deletion itself fails, an orphaned `auth.users` row is left with no trace. [apps/super-admin/app/(admin)/gyms/actions.ts:96,110,125] Source: Edge Case Hunter. **Fixed**: extracted a `deleteAuthUserAndLog` helper that checks the result and `console.error`s on failure; used in both cleanup call sites.
- [x] [Review][Patch] `gymNameExists()` passes the raw user-supplied name into `.ilike()` unescaped — a name containing `%` or `_` is treated as a SQL wildcard pattern, causing false-positive/false-negative duplicate-name detection. [apps/super-admin/services/gyms.ts:87-95] Sources: Blind Hunter, Edge Case Hunter. **Fixed**: escapes `%`/`_` before the `ilike` call.
- [x] [Review][Patch] `logGymCreated`'s `log_audit_event` RPC error is discarded — a failed audit-log write for gym creation disappears silently, undermining Story 1.4's append-only-audit-trail guarantee for the very first action type built on it. [apps/super-admin/services/gyms.ts] Sources: Blind Hunter, Edge Case Hunter. **Fixed**: checks the RPC's `error` and `console.error`s it (still doesn't throw/roll back — the gym/owner creation already succeeded).
- [x] [Review][Patch] `mapSupabaseError`'s email/phone-already-registered detection uses a loose string heuristic (`message.includes("email") && includes("already")`) instead of GoTrue's actual structured error `code` field — misclassifies unrelated errors as "already registered" and never detects a duplicate **phone** at all (falls to the generic fallback). Confirmed via this story's own manual testing that GoTrue returns structured codes (`phone_exists`, and by the same pattern `email_exists`) rather than requiring string-matching. [packages/types/src/errors.ts:20-32] Sources: Blind Hunter, Edge Case Hunter. **Fixed**: checks `error.code === 'email_exists'` / `'phone_exists'` directly; added `owner_phone_taken` mapping to `CreateGymModal`'s field-error handling too.
- [x] [Review][Patch] `mapSupabaseError`'s generic fallback discards the original error with no server-side logging, making "unknown" failures effectively undebuggable in production. [packages/types/src/errors.ts:34-37] Source: Blind Hunter. **Fixed differently than literally suggested**: `packages/types` targets ES2022 only (no DOM/Node lib, consumed by non-Node environments too), so `console` isn't type-available there — kept `mapSupabaseError` pure and added a `mapAndLog` wrapper in `apps/super-admin/services/gyms.ts` (where `console` is available) that logs whenever the mapped result is `code: 'unknown'`; used at every `mapSupabaseError` call site in the app.
- [x] [Review][Patch] `CreateGymModal.handleSubmit` has no try/catch/finally around `await createGym(...)` — if the action throws instead of resolving (unexpected exception, dropped connection), `submitting` never resets and the submit button is stuck on "Creating…" permanently. [apps/super-admin/app/(admin)/gyms/components/CreateGymModal.tsx:242-244] Sources: Blind Hunter, Edge Case Hunter. **Fixed**: wrapped in try/catch/finally; `finally` always resets `submitting`, `catch` shows a generic form error.
- [x] [Review][Patch] `createAdminClient()`'s non-null env-var assertions throw an opaque low-level error if `SUPABASE_SERVICE_ROLE_KEY`/`NEXT_PUBLIC_SUPABASE_URL` are misconfigured, and this call (Step 3 of `createGym`, after the gym row is already inserted) isn't guarded by the same try/cleanup pattern as the other failure branches — a misconfigured deployment would throw past the `{data,error}` contract, orphaning the just-inserted gym row (compounding the DELETE-policy finding above). [apps/super-admin/app/(admin)/gyms/actions.ts:56] Sources: Blind Hunter, Edge Case Hunter. **Fixed**: `createAdminClient()` + the `createUser` call are now wrapped together in a try/catch (via an IIFE that keeps TypeScript's inferred success type flowing cleanly into `admin`/`authUser`); any thrown error triggers the same `deleteGym` compensating cleanup as the other branches.
- [x] [Review][Patch] The toast `setTimeout` in `GymsPageClient` has no cleanup on unmount or re-trigger — risk of a `setState` call after unmount, or a stale timer clearing a newer toast early. [apps/super-admin/app/(admin)/gyms/components/GymsPageClient.tsx:21-26] Sources: Blind Hunter, Edge Case Hunter. **Fixed**: timer id stored in a ref, cleared before setting a new one and on unmount (`useEffect` cleanup).
- [x] [Review][Patch] `CreateGymModal`'s Cancel button, "✕" close button, and native Escape-to-close are not disabled while `submitting` is true — an admin can dismiss the modal mid-request while the Server Action (which may have already created an `auth.users` row) keeps running detached, with its eventual success/failure never shown. [apps/super-admin/app/(admin)/gyms/components/CreateGymModal.tsx] Source: Blind Hunter. **Fixed**: both buttons get `disabled={submitting}`; an `onCancel` handler on the native `<dialog>` calls `preventDefault()` while `submitting` to block Escape too.
- [x] [Review][Defer] Multi-step orchestration via 5 separate round-trips rather than one Postgres transaction — already the best achievable design given `auth.admin.createUser` is a GoTrue API call, not a SQL operation, so true single-transaction atomicity isn't possible regardless; the actionable mitigation is the DELETE-policy patch above, not a redesign. Source: Blind Hunter. — deferred, not actionable as stated
- [x] [Review][Defer] `is_super_admin()`/JWT claims have no DB-backed revocation check (a de-provisioned Super Admin retains access until token refresh) — pre-existing characteristic of the entire claims-hook system built in Story 1.3, not introduced by this diff. Source: Blind Hunter. — deferred, pre-existing
- [x] [Review][Defer] `idx_gyms_name_unique` isn't created `CONCURRENTLY` — real but inconsequential at current/pilot scale (NFR-009: 1-3 gyms), and Supabase migrations run inside a transaction where `CONCURRENTLY` typically isn't usable anyway. Source: Blind Hunter. — deferred, low severity at pilot scale
- [x] [Review][Defer] `listGyms()` has no pagination — a real scalability gap relative to the "hundreds of gyms" architectural target, and SA-02's own mockup shows pagination controls that weren't built this story. Natural fit for Story 1.6, which already extends the Gyms pages. Sources: Blind Hunter, Edge Case Hunter. — deferred to Story 1.6
- [x] [Review][Defer] Audit log stores the owner's phone (PII) in an immutable jsonb column with no redaction/retention policy — pre-existing, already-accepted tradeoff of the whole `audit_log` design from Story 1.4, not unique to this story. Source: Blind Hunter. — deferred, pre-existing Story 1.4 design tradeoff
- [x] [Review][Defer] AC #3's tenant isolation was verified via manual/pgTAP testing rather than a committed automated test exercising the real `createGym` code path end-to-end — consistent with this project's documented "no automated E2E dashboard testing in V1" standard and the lack of a browser-automation tool in this environment. Source: Acceptance Auditor. — deferred, matches project's V1 testing standard
- [x] [Review][Defer] A stale/deleted `tierId` at submit time would cause an FK violation mapped to the generic fallback error — not reachable today since Story 1.6 (tier deletion) hasn't shipped; revisit when it does. Source: Blind Hunter. — deferred, not yet reachable
- [x] [Review][Dismiss] "No authorization check inside `createGym` itself" — RLS is this project's explicitly chosen sole authorization layer (architecture.md), not defense-in-depth via app-code checks; the INSERT policy failing IS the deliberate enforcement mechanism. Source: Blind Hunter.
- [x] [Review][Dismiss] `getClaims()`'s "hard security boundary" comment is unverifiable — `getClaims()` performs real JWT signature verification (Supabase's documented behavior) and is the same pattern already used unmodified in this codebase's own `app/protected/page.tsx` (Story 1.1 scaffold). Source: Blind Hunter.
- [x] [Review][Dismiss] "No test coverage" — factually incorrect; 18 pgTAP assertions exist in `supabase/tests/gyms_super_admin_rls.test.sql`, inadvertently omitted from the diff supplied to this reviewer (a review-scoping error, not a code gap). Source: Blind Hunter.
- [x] [Review][Dismiss] Elite tier's sentinel `member_cap` not a named constant — already documented as a provisional decision in `docs/decisions.md` (Decision 4) and story Open Question 1. Source: Blind Hunter.
- [x] [Review][Dismiss] Suspended/Deactivated selectable as initial status at gym creation — matches the DB enum and SA-04's dropdown intent; not a defect. Source: Blind Hunter.
- [x] [Review][Dismiss] No DB guard against >1 `role='owner'` member per gym — not reachable via this diff's actual code paths (`insertOwnerMember` is always 1:1 with a freshly-inserted gym within the same function call; no path re-adds an owner to an existing gym). Source: Blind Hunter.
- [x] [Review][Dismiss] `linkData.properties.action_link` accessed without optional chaining — type-guaranteed non-null per the SDK's success-path type contract; not reachable without the SDK violating its own documented contract. Source: Edge Case Hunter.
- [x] [Review][Dismiss] `createGym` theoretically returning both `data` and `error` as null — not reachable; every actual return statement returns exactly one or the other, never both null. Source: Edge Case Hunter.
- [x] [Review][Dismiss] Zod issue on the `status` field not rendered as a field error — not reachable; the controlled `<select>`'s only possible values are exactly `gymStatusSchema`'s enum members, so `status` can never fail validation via the actual UI. Source: Edge Case Hunter.
- [x] [Review][Dismiss] Potential FK issue if a `members` row existed when `deleteGym` runs — verified not reachable in the current code (no failure branch calls `deleteGym` after a `members` row was successfully inserted). Source: Blind Hunter.

## Dev Notes

### Scope Boundary (read first)

This story delivers the Create Gym flow end-to-end (SA-02 minimal Gym List + SA-04 Create Gym modal) and the RLS/schema foundation it needs. It does **not**:
- Build tier CRUD, gym suspend/deactivate/reinstate, or Platform Metrics — all Story 1.6.
- Build the "Access gym data" escalation or any Super Admin read access to a gym's general member/payment data — Story 1.7 (FR-072). This story's `members` SELECT policy is narrowly scoped to `role = 'owner'` rows only (needed for SA-02/SA-03's Owner display) — it does not grant visibility into coach/receptionist/member rows.
- Build the Gym Owner's actual dashboard shell/sidebar (AD-01 login page already exists generically; the role-filtered Overview/sidebar experience is Story 1.8). AC #3 is satisfied by verifying the existing tenant-isolation mechanism (Story 1.3) correctly scopes a brand-new owner — not by building new dashboard UI.
- Send real SMS via any telecom provider — see Open Question 3. The SMS/OTP provider spike (Story 2.1, still backlog) is what validates Cameroon SMS deliverability; shipping real delivery in this story would build on an unverified provider, which the epics' own Additional Requirements explicitly warn against for the analogous Notch Pay/OTP cases.

### Open Questions for User/Architect Sign-Off

These weren't resolved by the PRD/epics/architecture and materially affect this story. Recommended defaults are already written into the Tasks above; flagging here in case you want to override before dev starts (same pattern as Stories 1.2/1.3/1.4):

1. **Elite tier's "no member cap" cannot be represented** — `tiers.member_cap` is `integer not null` (0002_gyms_and_tiers.sql), with no sentinel/nullable convention. Recommended: seed Elite with a large sentinel (`1000000`) for this story's bootstrap data, and treat the real schema fix (e.g., making `member_cap` nullable = unlimited) as Story 1.6's decision, since that story owns tier CRUD and can change the column without this story's migration needing to guess at UI/enforcement implications. Confirm the sentinel value is acceptable as a stopgap, or direct the dev to make `member_cap` nullable now instead (a small change, but one that also touches FR-086's member-cap-enforcement trigger design, which isn't built yet — Epic 2 — so there's no consumer to break today either way).

2. **Owner authentication identity: email vs. phone.** architecture.md states dashboard/Super Admin auth is "Supabase Auth email + password" (matching the already-scaffolded `login-form.tsx` in both `apps/dashboard` and `apps/super-admin`, which use `signInWithPassword({ email, password })`). But SA-04's mockup (EXPERIENCE.md) collects only Gym Name / Owner Name / **Owner Phone** / Tier / Status — no email field — for creating the owner account. There's no documented mechanism for an owner to later log in with `signInWithPassword({ email, ... })` if no email was ever collected. Recommended default (in Tasks above): add a required **Owner Email** field to the Create Gym form, deviating from SA-04's literal 3-field mockup, and create the owner's `auth.users` account with both `email` and `phone` set. This is the minimum change that makes AD-01/SA-01's existing, already-built login form actually work for a Story-1.5-created owner. Confirm this is acceptable, or direct an alternative (e.g., phone-based Supabase Auth with a different login form than the one already scaffolded — a larger change touching Story 1.8 too).

3. **SMS invite delivery has no validated provider yet.** Story 2.1 (SMS/OTP Provider Sandbox Spike, Epic 2, still backlog) is what proves Cameroon SMS deliverability via `TwilioSmsProvider`/`OtpDeliveryProvider`. This story's AC #1 requires "the owner receives an SMS with login instructions," and SA-04's spec requires the toast "Gym created. SMS sent to [number]." Recommended default: implement the send behind a small, swappable interface (e.g., `InviteSmsSender.send(phone, message)`) with **one concrete implementation for this story that does not call any real telecom API** — record the invite message via `log_audit_event()` metadata (and/or a console/Sentry breadcrumb) instead, and still show the SA-04-mandated toast copy, since the gym+owner creation itself did genuinely succeed. This avoids shipping unverified real SMS delivery in Epic 1 ahead of Epic 2's own gating spike, mirroring the project's existing risk posture for Notch Pay/OTP. **This has a real business-visible consequence** (a Super Admin will see "SMS sent" when no real SMS was sent) — confirm this tradeoff is acceptable, or direct the dev to pull forward a minimal real Twilio integration now (accepting the same un-spiked-provider risk the epics document otherwise avoids), or to change the toast copy to be honest about the current limitation.

### Technical Requirements & Architecture Compliance

- **RLS is the sole tenancy/role enforcement layer** (architecture.md Core Architectural Decisions) — every new capability in this story (Super Admin seeing all gyms, creating a gym, creating the owner's membership row) must be backed by an explicit RLS policy, not by routing routine operations through the service-role admin client. The **one and only** operation in this story that structurally requires the service-role admin client is `auth.admin.createUser` (Task 3) — there is no RLS-policy equivalent for creating an `auth.users` row. Keep that client's usage scoped to exactly that call.
- **Server Actions return `{ data, error }`, never throw for expected errors** (architecture.md Process Patterns) — `createGym` must follow this exactly; only genuine bugs should throw (caught by Sentry).
- **`packages/types` Zod schemas are the single source of validation** — do not redefine `createGymSchema`'s shape inline in the modal component or the Server Action.
- **snake_case at the DB boundary, camelCase only for pure UI-local state** — `createGym`'s input/output types follow this; the Zod schema's field names (`gymName`, `ownerPhone`, etc.) are UI-local until they cross into a `gyms`/`members` insert, at which point they map to `name`, `phone`, etc.
- **This is the first story to add real runtime code to `packages/types`** (previously `export {};` only, per Story 1.1's own review). The `transpilePackages` wiring flagged as deferred in that review's findings is now load-bearing, not optional.
- **The `(admin)` role-gate (Task 4) is a hard security boundary**, not incidental scaffolding — without it, any authenticated Supabase user (including a gym member/owner from the entirely separate `apps/dashboard` auth flow) could reach the Super Admin's Gyms page, since both apps share one Supabase project and Auth instance.

### Previous Story Intelligence

- **Table-level GRANTs are checked before RLS** — any new RLS policy added in Task 1 is only reachable because `0002`/`0003` already granted `authenticated` full CRUD on `gyms`/`tiers`/`members`; no new GRANT statements are needed this story, only policies. [Source: Story 1.3 Debug Log References, reconfirmed by Story 1.4]
- **`SECURITY DEFINER` + `set search_path = public`** is the established pattern for any function needing elevated privileges — not needed for `private.is_super_admin()` itself (it only reads `auth.jwt()`, same as `private.gym_id()`, no elevated access required), but is the pattern to follow if this story ends up needing any new privileged function.
- **`docs/decisions.md` dated-entry format** is established — one entry per decision, newest first, `**Decision N —**` heading per point. Follow exactly for Task 8.
- **`supabase test db` must actually be run**, not just statically reviewed — Stories 1.3 and 1.4 both found real bugs (UUID hex literals, pgTAP API misunderstandings, role-context mistakes in tests, a genuine logic bug in `log_audit_event()`) only by executing the suite. Story 1.4's dev environment note: Docker was unavailable from the primary shell but already running under WSL2 with the local Supabase stack up — check there first before assuming Docker is unavailable.
- **`log_audit_event()` (Story 1.4) is the canonical write path** for this story's audit record (Task 6, step 9) — call it, don't insert into `audit_log` directly (blocked by deny-all RLS for anything but `service_role` anyway). Its signature: `log_audit_event(p_action_type text, p_gym_id uuid default null, p_target_entity_id text default null, p_target_entity_type text default null, p_metadata jsonb default '{}'::jsonb, p_system_actor_label text default null) returns uuid`. It enforces `p_gym_id = private.gym_id()` for non-super-admin callers but exempts Super Admin — this story's caller is Super Admin creating a *new* gym, so `p_gym_id` (the new gym's id) will not match the caller's own `private.gym_id()` (which is null for Super Admin anyway) — this is exactly the exemption case the function already handles correctly; no changes to `log_audit_event()` needed.
- **This is the first story to build real application code in `apps/super-admin`** — everything under `app/`, `components/`, and `services/` beyond the `with-supabase` starter scaffold and the `/protected` demo page is new. There is no existing pattern in this repo yet for a Server Action, a service-layer file, or a role-gated layout — this story establishes those patterns for the app, so get them right; Story 1.6/1.7 will follow whatever precedent this story sets.

### Git Intelligence Summary

- HEAD is `10c6df8` (fix(story-1.4): close code review findings). Working tree is clean per `git status`. All of Stories 1.1-1.4's work (migrations 0001-0009, pgTAP tests, `docs/decisions.md`, `packages/types/src/database.ts`) is committed — this story builds on a clean, fully-landed base, unlike Story 1.4 which found prior stories' work still uncommitted.
- No commits since Story 1.4's code-review fix — this story is the next real work on this branch.

### Testing Standards

- pgTAP, `supabase/tests/*.test.sql`, run via `supabase test db` — same CI job as Stories 1.3/1.4, no new CI wiring needed.
- New: this is the first story with real application-layer (Next.js Server Action) logic that pgTAP cannot exercise. A manual end-to-end verification pass (Task 7) is required, not optional — this project has no automated E2E dashboard testing in V1 (per epics.md Testing Requirements, revisited at V1.5).

### Project Structure Notes

- Migration numbering deviates from architecture.md's illustrative sequence (which lists `0010_rls_policies_members.sql`, `0011_...payments`, etc., driven by Epic 2/4 feature stories) — this story's `0010_super_admin_gym_provisioning.sql` comes chronologically first since Epic 1 Story 1.5 ships before those Epics. Matches the precedent Stories 1.3/1.4 already set of deviating from architecture.md's literal file list with a recorded reason (docs/decisions.md).
- `apps/super-admin/app/(admin)/gyms/` matches architecture.md's Complete Project Directory Structure exactly (`app/(admin)/gyms/page.tsx`, `actions.ts`, `components/`). `services/gyms.ts` at the app root also matches.
- `packages/types/src/schemas/gym.ts` and `packages/types/src/errors.ts` match architecture.md's specified `packages/types` internal structure (`schemas/`, `errors.ts`) exactly — this story is simply the first to populate them.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 1.5: Super Admin — Create & Onboard a Gym] — story statement and ACs
- [Source: _bmad-output/planning-artifacts/epics.md#6.14 Super Admin Dashboard, FR-070–073, FR-086] — Super Admin capabilities, default tiers
- [Source: _bmad-output/planning-artifacts/epics.md#Additional Requirements] — RLS per-action policy strategy, "no ship on unverified provider" principle (applied here to the SMS decision)
- [Source: _bmad-output/planning-artifacts/prds/prd-gym_os-2026-06-20/prd.md#UJ-5, FR-007] — founder-assisted onboarding narrative ("sends the owner an SMS with their login credentials")
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-gym_os-2026-07-04/EXPERIENCE.md#SA-02, SA-03, SA-04] — Gym List/Detail/Create Gym layouts and on-submit behavior
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-gym_os-2026-07-04/EXPERIENCE.md#Field-Level Validation Rules — SA-04 Create Gym] — exact field rules/error copy
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-gym_os-2026-07-04/EXPERIENCE.md#State Patterns] — empty state, loading skeleton, error state copy for SA-02/SA-04
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-gym_os-2026-07-04/DESIGN.md] — brand tokens (primary/accent/background), Super Admin uses platform shell colors only
- [Source: _bmad-output/planning-artifacts/architecture.md#Core Architectural Decisions, Authentication & Security] — RLS as sole enforcement layer, email+password dashboard auth pattern (informs Open Question 2)
- [Source: _bmad-output/planning-artifacts/architecture.md#Complete Project Directory Structure] — `apps/super-admin/app/(admin)/gyms/`, `packages/types/src/schemas/`, `errors.ts` locations
- [Source: _bmad-output/implementation-artifacts/1-4-append-only-audit-log-foundation.md#Dev Notes, Debug Log References] — `log_audit_event()` signature/exemption behavior, Docker/WSL2 dev environment note, "run the suite for real" discipline
- [Source: _bmad-output/implementation-artifacts/deferred-work.md#story-1-1] — `packages/types` `transpilePackages` gap, explicitly predicted to matter "the moment it gains runtime code"
- [Source: supabase/migrations/0002_gyms_and_tiers.sql, 0003_members_and_users.sql, 0007_audit_log.sql, 0009_auth_hook_gym_claims.sql] — exact in-repo schema/RLS/grant conventions this story must match
- [Source: supabase/tests/rls_tenant_isolation.test.sql, auth_hook_canary.test.sql] — existing pgTAP session-simulation conventions (`set local role`, `set_config('request.jwt.claims', ...)`) to replicate for the new super_admin test cases
- [Source: apps/super-admin/components/login-form.tsx, apps/super-admin/app/protected/page.tsx] — confirms email+password login is already scaffolded and `auth.getClaims()` is the established claims-read pattern
- [Source: docs/decisions.md] — established dated-entry format

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5)

### Debug Log References

- **Local dev environment**: same as Story 1.4 — Docker unavailable from the primary Windows shell, but already running under WSL2 (Ubuntu) with the Supabase local stack up. Discovered mid-session that the WSL2 VM idle-shuts-down (and tears down the Docker stack with it) within seconds of no active WSL process, causing intermittent `ECONNREFUSED`/container-restart cycles between otherwise-independent tool calls. Worked around by holding a long-lived background `wsl.exe -e bash -lc "sleep 1200"` process for the duration of active verification work. Worth carrying forward to future stories as a known environment quirk.
- **`pnpm install` failed with `EACCES`** on `node_modules/.pnpm/node_modules/styled-jsx/package.json` after adding `zod` to `packages/types` — traced to a dangling/broken reparse point (an empty-target symlink) left over from a prior interrupted install, unrelated to this story's changes. Fixed by removing the broken symlink (a gitignored, fully regenerable build artifact) and re-running `pnpm install` successfully.
- **Real bug caught only by running `next dev`/`next build`, not `tsc --noEmit` or static review**: `apps/super-admin/next.config.ts` has pre-existing `cacheComponents: true` (Next.js 16 Cache Components/PPR, present since Story 1.1's scaffold, never previously exercised by any real dynamic Server Component). The Gym List page's cookie-based Supabase reads (`listGyms`/`listTiers`) triggered "Uncached data ... accessed outside of Suspense," which my own `{data,error}` handling silently converted into a generic "Something went wrong" render rather than crashing loudly — meaning `tsc` and even a successful Turbopack *compile* both looked clean while the page was actually broken at runtable. Only caught via a real HTTP request against the running dev server. Fixed by explicitly wrapping the data-fetching component in `<Suspense>` inside `page.tsx` (relying on the route's `loading.tsx` alone was insufficient).
- **Pre-existing, unrelated typecheck error confirmed and left alone**: `apps/super-admin/app/layout.tsx:30` (`ThemeProvider` `children` prop type mismatch) fails `tsc --noEmit`/`next build`'s typecheck phase. Confirmed via `git diff --stat` and `git log` that this file has zero diff and was last touched in Story 1.1's initial scaffold commit (`3ec4a03`) — predates this story entirely, not introduced or touched by any of my changes. Not fixed (out of scope); `next dev` is unaffected (dev mode doesn't hard-block on TS errors the way `next build`'s typecheck phase does), so it didn't block manual verification.
- **Manual end-to-end verification methodology** (no browser-automation tool available): created a real `super_admin`-flagged test user (`superadmin@gymos.test`) via the GoTrue Admin API + a direct `is_super_admin=true` update, local-only. Wrote a temporary Node script (`apps/super-admin/_verify_1_5.mjs`, deleted before completion, never committed) that: (1) signed in for real via the password grant to get a genuine GoTrue-issued JWT and confirmed `app_role: super_admin` was actually present in its claims; (2) constructed the exact `@supabase/ssr` session cookie format (`base64-` + base64url JSON, cookie name `sb-127-auth-token` — derived from `@supabase/supabase-js`'s `defaultStorageKey` logic for a `127.0.0.1` host) and used it to hit the running dev server's `/gyms` route directly, confirming the role-gate passes for a real session and the page actually renders (not just compiles); (3) confirmed an unauthenticated request redirects to `/auth/login` (307); (4) exercised the full `createGym` sequence (gym insert → `auth.admin.createUser` → `auth.admin.generateLink` → owner `members` insert → `log_audit_event`) directly via `supabase-js` using the real authenticated session + the admin client, proving the RLS policies and Admin API calls compose correctly for a real request; (5) verified the negative path (duplicate case-varied gym name rejected by the unique index) and the compensating-cleanup trigger condition (a forced non-owner `members` insert correctly rejected by the `WITH CHECK` clause); (6) signed in as the newly created owner via the real recovery-link token (mirroring what clicking the emailed link does) and confirmed their JWT's `gym_id` claim matched the new gym, and a direct `gyms` SELECT under that session returned exactly one row — their own, proving AC #3 through the real code path, not synthetic pgTAP claims. All test data (gyms, auth users, audit log rows) cleaned up afterward via direct `psql`.
- **Two real pgTAP test-authoring bugs**, caught only by actually running `supabase test db`, not by writing/reviewing the test file statically: (1) asserted `super_admin` sees exactly 1 tier after seeding 1 in the test — forgot `0010_super_admin_gym_provisioning.sql` itself seeds 3 default tiers (FR-073), so the real count is 4; (2) asserted `super_admin` sees exactly 1 `members` row (the one just inserted) — forgot the role='owner' SELECT policy is scoped platform-wide, not to "rows this session created," so the pre-seeded Gym A owner (seeded directly as `postgres`, before any RLS) is also visible, making the real count 2. Both fixed with corrected expected values and clarifying assertion descriptions.

### Completion Notes List

- All 8 tasks implemented: migration `0010_super_admin_gym_provisioning.sql` (helper function, unique index, RLS policies, default tier seed); `packages/types` gained its first runtime code (Zod schema, error mapping, `transpilePackages` wiring); a scoped service-role admin client; a hard role-gated `(admin)` route group; the SA-02 Gym List page; the SA-04 Create Gym modal + `createGym` Server Action with full compensating-cleanup logic; 18 new pgTAP assertions (74 total, all passing); a 6-decision `docs/decisions.md` entry.
- All three Open Questions flagged in Dev Notes were resolved using their recommended defaults, and two of the three (Owner Email/dual-identity, SMS stub) were **confirmed necessary through actual end-to-end testing**, not just theoretical justification — see Debug Log References.
- **Mid-implementation design refinement** (not in the original story draft): added a `members` SELECT policy scoped to `role = 'owner'` only, discovered necessary while building the Gym List page (SA-02/SA-03's "Owner: <name>" display is unreachable under a pure INSERT-only `members` policy). Deliberately kept narrow and distinct from Story 1.7's audit-logged escalation — documented in `docs/decisions.md` Decision 3 and reflected in Task 1's checklist text.
- **Service-layer boundary refined during implementation**: the story's Task 5 originally described a `createGym` function living in `services/gyms.ts`; actual implementation split this so `services/gyms.ts` holds only thin CRUD primitives (matching architecture's "no repository abstraction" service-layer pattern) while the full orchestration (validation, admin-client calls, compensating cleanup, audit log, SMS) lives in `app/(admin)/gyms/actions.ts` as the Server Action — matching architecture's stated boundary ("business-logic operations go through Server Actions") more precisely than the story's first draft.
- Local dev/testing environment (`apps/dashboard/.env.local`, `apps/super-admin/.env.local`) was switched from the remote Supabase project to local Supabase for this story's work, since the remote project has never had any migration pushed to it — all schema/RLS work in this project has only ever existed locally (Stories 1.2-1.4's own precedent). Both `.env.local` files are gitignored and retain a comment with the original remote values for easy revert. Not part of the story's tracked File List since gitignored, but noted here for continuity.
- A local-only `super_admin` test account (`superadmin@gymos.test` / `SuperAdmin123!`) was created directly against the local Supabase instance for manual verification. Not part of any migration/seed (deliberately — a real production seed of a super-admin credential would be a security smell); it will be wiped on the next `supabase db reset` and isn't tracked anywhere in git. Future stories needing a super_admin session for manual testing will need to recreate one the same way (GoTrue Admin API `createUser` + `UPDATE public.users SET is_super_admin = true`).
- `packages/types/src/database.ts` was regenerated via `supabase gen types typescript --local` and diffed — **byte-identical**, since this story added only RLS policies, an index, and seed data, none of which change any table's generated row shape. No file update was needed or made.

### File List

- `supabase/migrations/0010_super_admin_gym_provisioning.sql` (new)
- `supabase/tests/gyms_super_admin_rls.test.sql` (new)
- `packages/types/package.json` (modified — added `zod` dependency)
- `packages/types/src/schemas/gym.ts` (new — `createGymSchema`)
- `packages/types/src/errors.ts` (new — `mapSupabaseError`)
- `packages/types/src/index.ts` (modified — first real exports, replacing `export {};`)
- `apps/super-admin/next.config.ts` (modified — added `transpilePackages: ['@gymos/types']`)
- `apps/super-admin/.env.example` (modified — added `SUPABASE_SERVICE_ROLE_KEY` placeholder/docs)
- `apps/super-admin/lib/supabase/admin.ts` (new — service-role admin client factory)
- `apps/super-admin/app/(admin)/layout.tsx` (new — role-gated route group)
- `apps/super-admin/app/(admin)/gyms/page.tsx` (new — SA-02 Gym List, Server Component + Suspense)
- `apps/super-admin/app/(admin)/gyms/loading.tsx` (new — SA-02 loading skeleton)
- `apps/super-admin/app/(admin)/gyms/actions.ts` (new — `createGym` Server Action)
- `apps/super-admin/app/(admin)/gyms/components/GymsPageClient.tsx` (new — table, empty state, toast)
- `apps/super-admin/app/(admin)/gyms/components/CreateGymModal.tsx` (new — SA-04 modal)
- `apps/super-admin/services/gyms.ts` (new — thin service-layer CRUD primitives)
- `docs/decisions.md` (modified — added 2026-07-08 Story 1.5 entry, 6 decisions)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (modified — status tracking)
- `_bmad-output/implementation-artifacts/deferred-work.md` (modified — added story 1-5 code-review deferred items)
- `pnpm-lock.yaml` (modified — `zod` dependency added)

Not tracked (gitignored, local-dev-only, documented in Completion Notes): `apps/dashboard/.env.local`, `apps/super-admin/.env.local` (both switched to point at local Supabase), `apps/super-admin/.env.local`'s new `SUPABASE_SERVICE_ROLE_KEY`.

## Change Log

- 2026-07-08: Implemented Super Admin gym provisioning end-to-end — migration `0010_super_admin_gym_provisioning.sql` (`private.is_super_admin()` helper, case-insensitive gym-name unique index, Super Admin RLS policies on `gyms`/`tiers`/`members`, default tier seed data per FR-073); `packages/types`' first runtime code (`createGymSchema`, `mapSupabaseError`); a scoped service-role admin client; a role-gated `(admin)` route group (a hard security boundary — previously any authenticated user could reach Super Admin pages); the SA-02 Gym List page and SA-04 Create Gym modal + `createGym` Server Action with full compensating-cleanup logic on partial failure. Refined the `members` RLS design mid-implementation to add a narrow `role='owner'`-scoped SELECT policy after discovering SA-02/SA-03's Owner display was otherwise unreachable, keeping it deliberately distinct from Story 1.7's audit-logged escalation. Found and fixed a real `cacheComponents`/Suspense bug that silently broke the Gym List page's data fetching, caught only by running the actual dev server. 18 new pgTAP assertions written and passing (74 total). Full manual end-to-end verification performed against local Supabase, including a real login as a newly created gym owner to prove AC #3's tenant isolation through the actual code path. 6-decision entry recorded in `docs/decisions.md`.
- 2026-07-09 (code review): Ran `bmad-code-review` with 3 parallel adversarial layers (Blind Hunter, Edge Case Hunter, Acceptance Auditor) against the diff. All three layers independently found the same most-severe issue: no `DELETE` RLS policy existed on `gyms`, so `deleteGym()`'s compensating cleanup silently matched 0 rows on every partial-failure path, permanently orphaning ownerless gym rows. Resolved the one decision-needed finding (the "SMS sent" toast lying about stub delivery) by making the copy honest, threading a new `smsSent` flag through `createGym`'s return value. Applied all 11 patch findings: added a `gyms` DELETE policy scoped to orphaned (memberless) gyms only, with `deleteGym()` now verifying the delete actually took effect; checked previously-ignored errors in `deleteUser`, `log_audit_event`, and the generic error-mapping fallback (added a `mapAndLog` wrapper in the app layer, since `packages/types` can't depend on `console`); escaped `ilike` wildcard characters in the gym-name pre-check; switched `mapSupabaseError` to GoTrue's structured `code` field (`email_exists`/`phone_exists`) instead of a loose string heuristic, fixing a real gap where duplicate-phone errors were never detected; guarded `createAdminClient()`'s call site with the same compensating-cleanup pattern as the other failure branches; added try/catch/finally around `CreateGymModal`'s Server Action call so an unexpected exception can't permanently disable the submit button; disabled the modal's close affordances (button, ✕, Escape) while a request is in flight; fixed a `setTimeout` cleanup gap in the toast. Added 4 new pgTAP assertions proving the new DELETE policy's exact scope (78 total, all passing) and re-verified the fix manually end-to-end (forced the failure path again, confirmed both the gym and the auth user are now genuinely removed, not just filtered from view). 7 items deferred to `deferred-work.md` (pre-existing/out-of-scope items: JWT revocation, index CONCURRENTLY, pagination, audit-log PII, orchestration redesign, AC #3 automated-test gap, stale-tierId edge case). 9 findings dismissed as noise (verified unreachable, already-documented, or based on a false premise about this project's chosen RLS-only authorization model).
