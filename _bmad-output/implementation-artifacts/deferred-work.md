# Deferred Work

## Deferred from: code review (Round 2) of story-6-1-expo-push-token-registration-cleanup (2026-08-04)

- No `CONCURRENTLY` on the new `idx_device_push_tokens_expo_push_token` index [supabase/migrations/0043_add_idx_device_push_tokens_expo_push_token.sql:4] — takes an ACCESS EXCLUSIVE lock during build; harmless today on a brand-new, empty table, and not a regression (only one other migration in this codebase's entire history — 0039 — ever uses `CONCURRENTLY`).

## Deferred from: code review of story-5-3-coach-portal-member-detail-session-notes (2026-08-02)

- `router.push` used for both sort-column clicks and debounced search input, polluting browser history on every interaction [apps/dashboard/app/(dashboard)/coach/components/CoachPortalPageClient.tsx] — reclassified from patch to defer during apply: verified byte-identical `router.push(...)` (not `replace`) exists in the same `updateParams`-style function in `MembersPageClient.tsx`, `SubscriptionsPageClient.tsx`, and `AttendancePageClient.tsx` — a deliberate, app-wide convention across all four filter/sort pages, not a regression introduced by this story. Fixing only the coach page would make it the one inconsistent page; worth a cross-cutting follow-up across all four if this behavior is ever revisited.
- `select ... into v_coach_id` without `strict` in both `add_session_note`/`edit_session_note` silently picks a row rather than erroring on an unexpected duplicate match [supabase/migrations/0041_coach_portal_member_detail_session_notes.sql] — deferred, pre-existing: mirrors migration 0039's `assign_coach()` convention; fixing in isolation here would diverge from that established pattern.
- `noteTimestamp()` calls `toLocaleDateString`/`toLocaleTimeString` with no explicit `timeZone` in a `"use client"` component still SSR'd on first pass — a hydration-mismatch risk if server/browser timezones differ [apps/dashboard/app/(dashboard)/coach/[memberId]/components/CoachMemberDetailPageClient.tsx] — deferred, pre-existing: shares the same pattern as the codebase's existing `formatLocalDate` helper, used unchanged elsewhere; a cross-cutting fix, not scoped to this story.
- `apps/dashboard/locales/{en,fr}.json` diff includes unrelated `members.*` key additions with no corresponding component changes in this story's scope [apps/dashboard/locales/en.json, apps/dashboard/locales/fr.json] — deferred, pre-existing: leftover from separate uncommitted work touching the same shared locale files, not authored by this story.

## Deferred from: code review of story-5-2-coach-portal-assigned-member-list (2026-08-02)

- `sort`/`dir` query params are passed to `CoachPortalPageClient` unvalidated, so a hand-edited invalid value can desync the `aria-sort`/arrow indicator from the actual applied query order [apps/dashboard/app/(dashboard)/coach/page.tsx:59-60] — deferred, pre-existing: `subscriptions/page.tsx:74`, the exact template this story mirrors, already passes `params.sort` through with the same lack of validation.
- `private.is_assigned_coach()` is `SECURITY DEFINER` with no self-contained tenant check on `p_member_id` — safety currently depends on every calling RLS policy also independently checking `gym_id = private.gym_id()` on the target row before calling it [supabase/migrations/0040_coach_portal_member_list_rls.sql] — deferred, matches the spec-mandated function shape exactly and is safe under all current call sites (both new policies check `gym_id` first); a note for whoever adds a future caller of this helper.
- No uniqueness constraint on `coach_assignments` prevents two coaches from simultaneously holding a non-ended assignment to the same member [supabase/migrations/0039_coach_member_assignment.sql] — pre-existing from Story 5.1, out of scope for this story.
- A member with a real `coach_assignments` row but no `subscriptions` row is silently invisible to their coach, since `listAssignedMembers` reads exclusively from `subscriptions_current` [apps/dashboard/services/coaches.ts:230-234] — deferred, inherent to the `subscriptions_current` view this story was explicitly directed to reuse (Scope Notes); matches an already-accepted gap pattern elsewhere in this codebase.

## Deferred from: code review of story-5-1-coach-member-assignment (2026-08-02)

- Concurrent reassignment race: two simultaneous `assign_coach()` calls could both pass the "no active row" check under READ COMMITTED and then collide on `idx_coach_assignments_active_member` during INSERT, surfacing an unmapped Postgres unique-violation error instead of a friendly message [supabase/migrations/0039_coach_member_assignment.sql] — mirrors `renew_subscription()`'s identical pre-existing shape (no advisory lock), an inherited codebase-wide design pattern, not something newly introduced by this diff.

## Deferred from: code review of story-4-9-member-app-payment-history-receipt-detail (2026-08-01)

- `getPaymentReceipt`'s companion `gymResult` query (`supabase.from('gyms').select('name').single()`) has no explicit filter, unlike every other query added in this diff, which all add a defense-in-depth `.eq(...)` per their own comments [apps/mobile/src/app/(tabs)/history/payment/[id].tsx:997] — relies solely on RLS for single-row scope; matches a pre-existing pattern already used elsewhere in this app (e.g. `(tabs)/index.tsx`'s own `loadHome`, `profile.tsx`), not unique to this story.
- `getRecentPayments` has no secondary `order by id` tie-breaker at its `.limit()` boundary, unlike `loadPaymentsPage` in the same file, which explicitly orders by `(created_at desc, id desc)` for exactly this reason [apps/mobile/src/services/payments.ts:1363-1370] — deferred, pre-existing gap: `getRecentCheckIns` (the function this one is explicitly modeled on) has the identical missing tie-breaker.
- The Home screen's merged-activity sort comparator (`new Date(bTime).getTime() - new Date(aTime).getTime()`) has no fallback for a date-parse failure (NaN), which would produce unspecified `Array.prototype.sort` ordering behavior [apps/mobile/src/app/(tabs)/index.tsx:634-638] — requires malformed timestamp data from the DB to trigger; no other screen in this app guards a raw `new Date().getTime()` comparator either.

## Deferred from: code review of story-4-8-subscriptions-page-manual-renewal (2026-08-01)

- Duplicate-named FK relationship synthesized in `database.ts` for the new view — regenerating types added a **second** `payments_subscription_id_fkey` relationship entry (same constraint name as the existing one, but `referencedRelation: "subscriptions_current"` instead of `subscriptions`), which `supabase gen types` inferred from the view's `subscription_id` column [packages/types/src/database.ts:585-595] — deferred, likely harmless: PostgREST embed resolution uses real DB constraints in `pg_catalog`, not this generated client-type file, so no runtime ambiguity is expected; the Dev Agent Record's Debug Log claim that the diff was "limited to" the expected changes has been corrected to note this. Revisit if any future PostgREST embed query against `payments` behaves unexpectedly.
- No TypeScript unit-test coverage for the new service-layer functions (`listSubscriptions`, `exportSubscriptionsCsv`, `applySubscriptionFilters`, sort-column resolution) [apps/dashboard/services/subscriptions.ts] — matches this entire codebase's established convention (zero `.test.ts` files exist under `apps/dashboard`; pgTAP + typecheck + hands-on verification is the documented testing standard per this story's own Dev Notes).
- `exportSubscriptionsCsv`'s count-then-fetch has a TOCTOU race — a row inserted between the `head:true` pre-check and the capped data query could push the real count past 1,000 while the export silently returns 1,000 rows instead of `export_too_large` [apps/dashboard/services/subscriptions.ts:365-388] — byte-identical to `exportMembersCsv()`'s already-shipped pattern in `apps/dashboard/services/members.ts`, which this story was explicitly instructed to mirror.
- `status`/`planType` URL params are passed to the client `<select>` unvalidated [apps/dashboard/app/(dashboard)/subscriptions/page.tsx:72-73] — identical unvalidated pass-through already exists in `members/page.tsx:72`; the server-side filter itself is safely allowlisted (`applySubscriptionFilters`).
- A stale `page` URL param beyond the actual page count shows "no subscriptions match your filter" even when no filter is set and rows exist on other pages, because the empty-state condition ignores `page` entirely [apps/dashboard/app/(dashboard)/subscriptions/components/SubscriptionsPageClient.tsx:205-211] — byte-identical condition already ships in `MembersPageClient.tsx:229`.
- The new back-date error mapping matches on raw `raise exception` substrings duplicated between the SQL text and the TS string match, with nothing keeping them in sync if either changes later [packages/types/src/errors.ts] — this is the codebase's established error-mapping convention, used throughout `errors.ts` for every other RPC's raised exceptions.
- `subscriptions_current`'s `distinct on` scans across all gyms before any `gym_id` filter is applied by the caller, with no supporting index added in this migration [supabase/migrations/0037_subscriptions_page_manual_renewal.sql:35-56] — real but speculative without production query-plan/volume data; this is the codebase's first view, so no prior indexing precedent exists to compare against.
- `csvEscape()`'s OWASP CSV-injection guard only checks a leading `=+-@`, not a leading tab/CR (also commonly-cited formula-injection triggers) [apps/dashboard/services/subscriptions.ts:336-342] — byte-identical to `members.ts`'s own `csvEscape()`, which this story was explicitly instructed to copy verbatim (per-file-copy convention); found in an independent round-2 review.
- `confirm_renewal()`'s current-subscription lookup has no `for update` row lock — two concurrent renewal calls for the same member could both read the same "current" row and both insert a new subscription [supabase/migrations/0037_subscriptions_page_manual_renewal.sql:105-118] — pre-existing select shape from 0036, untouched by this story's own "byte-for-byte identical to 0036 — do not touch" instruction for this section; found in an independent round-2 review.
- `subscriptions_current` exposes `deactivated_at` as a plain column with no `where deactivated_at is null` in the view itself, relying entirely on caller discipline [supabase/migrations/0037_subscriptions_page_manual_renewal.sql:44-67] — real but speculative, no current consumer is affected; this is the codebase's first view so no prior filter-in-view-vs-filter-in-query precedent exists; found in an independent round-2 review.

## Deferred from: code review of story-4-7-inline-renewal-panel (2026-08-01)

- `RecordRefundModal.tsx`'s `listRefundEligiblePaymentsAction` has no promise-rejection handling — an unexpected throw leaves the payment list stuck on "Loading payments…" forever with no error shown [apps/dashboard/app/(dashboard)/payments/components/RecordRefundModal.tsx] — deferred, pre-existing (Story 4.5 file, only touched by this story's dialog-theming className addition).
- `payments.refundModal.errors.amountExceedsPayment` i18n key is added but has no wired-up consumer — `RecordRefundModal.tsx`'s `FIELD_ERROR_KEY` map only wires up `amountInvalid`/`reasonInvalid` [apps/dashboard/app/(dashboard)/payments/components/RecordRefundModal.tsx] — deferred, pre-existing (Story 4.5 file).
- `docs/decisions.md`'s `NormalizedPaymentEvent.vendor` widening (to accommodate `mapTaraMoneyVendor`'s new normalized output) is currently unused downstream, confirmed via grep [supabase/functions/payment-webhook/_shared/payment-providers/PaymentProvider.ts] — deferred, already self-documented and justified as forward-looking hygiene alongside the payment-method-opening change; not blocking.

## Deferred from: code review of story-4-6-real-time-front-desk-alert (2026-08-01)

- Auto-dismiss (AC #4, default 30 min) is implemented purely as a per-session client-side `setTimeout` [apps/dashboard/components/shared/FrontDeskAlertPanel.tsx:156-168] — no server-side job backs it, so if no staff browser has Overview/Attendance mounted when the window elapses, the alert doesn't auto-dismiss on schedule (it fires late, next time any session opens the panel). Deferred: out of story scope, a server-side cron job is a larger architectural addition than this story's scope; the existing `run_check_in_auto_timeout_job`/`run_subscription_lifecycle_job` pattern is the template for a future story to follow.

## Deferred from: code review of story-4-5-refund-recording (2026-08-01)

- `refund_recording.test.sql` only seeds/tests one of the three enumerated "not verified" statuses (`pending`) though the story's Task 8 literally lists `pending`/`processing`/`flagged` [supabase/tests/refund_recording.test.sql] — functionally redundant since the RLS policy's `p.status = 'verified'` check is a single boolean gate that treats all three non-verified values identically (proven equivalent by construction), and the test does exercise that boundary; a literal gap against the task's enumerated wording rather than a functional coverage hole.

## Deferred from: code review of story-4-4-payment-reconciliation-discrepancy-flagging (2026-08-01)

- Webhook-before-ref-persisted race produces a permanent, un-healing `missing_internal_record` flag [supabase/functions/payment-webhook/index.ts:226-235, handleInitiate lines 151-162] — the webhook lookup by `provider_transaction_ref` can run before `handleInitiate`'s separate, later, up-to-3-retry write persists that same ref; if so, `matched_payment_id` stays `null` forever and the resulting `missing_internal_record`/`stale_processing` discrepancy rows never self-heal, even after the ref eventually persists — accepted as a V1 gap, consistent with this codebase's existing append-only "no resolve action for V1" convention (Story 3.1's job-failure list never auto-clears either); documented in `docs/decisions.md`.
- `amount_mismatch` detection and UI never compare/show `currency` [supabase/migrations/0032_payment_reconciliation_job.sql, apps/dashboard/services/payments.ts, PaymentsPageClient.tsx] — currently a dead path since `payments.currency`/`payment_webhook_events.currency` are always `'XAF'` app-wide (no multi-currency flow exists anywhere in the product yet), but a same-numeric/different-currency mismatch would go fully undetected if that ever changes.
- New FKs (`payment_webhook_events.provider_key`/`.matched_payment_id`, `payment_discrepancies.payment_id`/`.webhook_event_id`) lack an explicit `ON DELETE` clause [supabase/migrations/0032_payment_reconciliation_job.sql] — not reachable through any delete path in this diff (the only `payments` row deletion, in `handleInitiate`'s failure cleanup, always runs before a webhook/discrepancy row could exist for that payment), but a landmine for any future payments-deletion path (GDPR/data-retention, admin cleanup).
- `payment_webhook_events.status` CHECK constraint only permits `('verified', 'flagged')` while `NormalizedPaymentEvent.status`'s type still allows `"processing"` [supabase/migrations/0032_payment_reconciliation_job.sql, supabase/functions/payment-webhook/_shared/payment-providers/PaymentProvider.ts:45] — currently unreachable since the sole real provider (`TaraMoneyProvider`) only ever emits `verified`/`flagged`, but a future second provider (AC #4) reporting anything else would throw on insert, get silently swallowed by the non-blocking `console.error`, and permanently defeat AC #1 for that event.

## Deferred from: story-4-4-payment-reconciliation-discrepancy-flagging (2026-07-31)

- No Super Admin (or any) UI surfaces `missing_internal_record` discrepancies — `run_payment_reconciliation_job()` writes the row (`payment_discrepancies`, `gym_id = null`), but nothing displays it [supabase/migrations/0032_payment_reconciliation_job.sql, apps/dashboard/app/(dashboard)/payments/*] — deliberate, reasoned V1 gap, not an oversight: TaraMoney's webhook payload carries a single platform-wide `businessId`, not a per-gym identifier, so this discrepancy type can never be attributed to a gym; the gym-scoped RLS policy makes it structurally invisible on every gym's own Payments page by design, and no FR/mockup/story slot backs a platform-wide unattributed-webhook viewer (unlike Story 3.1's `super_admin_job_failures()`, which had an explicit architecture.md mandate). See `docs/decisions.md` (2026-07-31, Story 4.4 entry) for the full reasoning.

## Deferred from: code review of story-4-3-manual-payment-entry-verification-queue (2026-07-31)

- `initiatePayment` (Story 4.2, untouched by this diff) can leave an orphaned `processing` row on a network failure between the INSERT and `functions.invoke()` [apps/dashboard/services/payments.ts] — pre-existing Story 4.2 gap; the docstring's claim that "the row is already deleted by the initiate route itself" only covers a returned error, not a client-side network failure before the call ever lands.
- `gym_staff_insert_own_payments`'s `with check` doesn't validate `amount > 0` or method/status coherence [supabase/migrations/0031_manual_payment_verification_queue.sql] — consistent with this same diff's own precedent above (INSERT policy not verifying `member_id` belongs to `gym_id`) of relying on the service layer/Zod schema rather than duplicating validation in RLS.
- `/payments` page shell renders for Coach before RLS blocks data [apps/dashboard/app/(dashboard)/payments/page.tsx] — pre-existing app-wide convention; `page.tsx`'s own comment notes the Sidebar hides the nav entry and RLS is "the real gate," matching every other role-restricted page in this codebase (no client-side route guards exist anywhere).

## Deferred from: code review of story-4-2-notch-pay-payment-integration (2026-07-31)

- No safeguard against duplicate/concurrent payment initiation — two concurrent POSTs to the initiate route for the same `paymentId` can both pass the eligibility check and both trigger a real charge (no row lock/atomic claim between check and provider call), and nothing stops `initiatePayment` being called twice in quick succession for the same member, each firing its own charge [supabase/functions/payment-webhook/index.ts — handleInitiate; apps/dashboard/services/payments.ts — initiatePayment] — deferred: no UI calls `initiatePayment` yet, Story 4.7 (Inline Renewal Panel) is the first real caller, so this isn't reachable until that UI ships; belongs with 4.7's own click-to-charge flow design (e.g. a partial-unique-index limiting one in-flight `processing` payment per member, mirroring `idx_attendance_events_one_open_per_member`).
- Webhook-receive path never reconciles the provider-reported amount against the payment's original amount — the post-verification lookup selects only `id` from `payments`, so `complete_verified_payment` accepts whatever fee/amount a signature-valid webhook asserts with no cross-check against what was actually initiated [supabase/functions/payment-webhook/index.ts] — deferred: webhooks are already signature-verified via shared secret, making this defense-in-depth rather than an exploitable gap under TaraMoney's real observed behavior; revisit if a real amount mismatch is ever observed.
- Member-selected `method` (mtn_momo/orange_money) is never actually sent to TaraMoney (`network` hardcoded to `""`) and never reconciled against the webhook's independently-derived vendor (`event.vendor`) [supabase/functions/payment-webhook/_shared/payment-providers/TaraMoneyProvider.ts, index.ts] — deferred: Story 4.1's real spike evidence shows TaraMoney correctly derives the vendor from the phone number itself regardless of `network`, so current behavior appears correct in practice; revisit only if a real method/vendor mismatch is ever observed.
- `gym_staff_insert_own_payments` INSERT policy doesn't verify `member_id` belongs to `gym_id` [supabase/migrations/0030_payment_initiation_and_renewal.sql] — pre-existing pattern inherited verbatim from `manager_or_owner_insert_own_subscriptions` (0018); the intended service-layer path (`initiatePayment`'s gym-scoped subscription lookup) already prevents exploiting it through the normal flow.
- "Most recent subscription" lookups (pricing in `initiatePayment`, renewal-plan resolution in `complete_verified_payment`) don't filter by subscription status, so a later-created but cancelled/expired row could in theory dictate price/plan [apps/dashboard/services/payments.ts, supabase/migrations/0030_payment_initiation_and_renewal.sql] — this story's own Dev Notes explicitly instruct reusing `renew_subscription()`'s (0022, Story 3.2) exact query pattern verbatim, so any gap here predates this story.
- Renewal expiry math (`current_date + duration_days`) ignores remaining time on a not-yet-expired subscription — no `greatest(current_date, previous_expiry_date)` logic [supabase/migrations/0030_payment_initiation_and_renewal.sql] — mirrors `renew_subscription()`'s established behavior per this story's own explicit instructions, not a new gap.
- Failed/declined ("flagged") webhook deliveries leave the `payments` row at `processing` forever rather than transitioning it to the schema's existing `flagged` enum value [supabase/functions/payment-webhook/index.ts] — this story's own Dev Notes explicitly assign this to "Story 4.4's reconciliation-job concern, not this story's."
- Generated `database.ts` type for `complete_verified_payment`'s `p_fee_amount` argument is non-nullable, but the webhook handler calls it with `null` whenever a fee isn't derivable [packages/types/src/database.ts] — a Supabase codegen fidelity gap (Postgres arg nullability doesn't round-trip), compounded by the already-documented absence of any typecheck coverage for Edge Function (Deno) code; not something to hand-patch in a generated file.
- `authorizationUrl` computed by the Edge Function's initiate route is never read or returned by `apps/dashboard/services/payments.ts`'s `initiatePayment` (which only returns `{ paymentId }`) [apps/dashboard/services/payments.ts] — dead for the current TaraMoney/USSD-only flow (TaraMoney never populates it for MTN/Orange), relevant only if/when a future redirect-based provider is registered.

## Deferred from: code review of story-4-1-notch-pay-sandbox-spike (2026-07-31)

- `originalAmount` (TaraMoney's fee-adjusted amount) is parsed off the real webhook payload and commented as "FR-039 fee-passthrough relevance," but never propagated to `NormalizedPaymentEvent` or persisted anywhere [supabase/functions/payment-webhook/_shared/payment-providers/TaraMoneyProvider.ts:49] — real fee-passthrough handling is Story 4.2+'s job; flagged so this field-availability finding isn't rediscovered from scratch.
- `payments.provider_transaction_ref` carries one global `unique` constraint (pre-existing, `0005_payments.sql`), not scoped per-provider — a future second real gateway producing a colliding reference string could silently collide via this story's upsert `onConflict` target [supabase/migrations/0005_payments.sql:14, supabase/functions/payment-webhook/index.ts:106-123] — only one real provider exists today so it isn't reachable yet; worth a composite-uniqueness follow-up once a second gateway is added.
- `payment_providers` grants table-level INSERT/UPDATE/DELETE to `service_role` (which conventionally bypasses RLS in Supabase) despite the "single blessed write path via `activate_payment_provider()`" design narrative [supabase/migrations/0029_payment_provider_registry.sql:26] — identical to the baseline GRANT convention already used on every other table in this repo (tiers, gyms, users, payments, etc.), not unique to this diff.
- `sprint-status.yaml` also flips Story 3.10 to `done` in this same diff, unrelated to Story 4.1 [_bmad-output/implementation-artifacts/sprint-status.yaml] — diff-hygiene note only, not a code defect.

## Deferred from: code review of story-3-10-member-app-plan-details-check-in-history (2026-07-30)

- Unfiltered `.single()` on `gyms` table assumes exactly one row exists [apps/mobile/src/app/(tabs)/history/index.tsx:110] — identical pre-existing pattern in `(tabs)/index.tsx:118` and `profile.tsx:78`; not introduced by this story.
- New "History" tab label is hardcoded English [apps/mobile/src/components/app-tabs.tsx:29] — every other tab label (Home/Check In/Profile) in this same file is also hardcoded; a pre-existing app-wide gap, not unique to this story's new tab.
- Async load failures are silently swallowed with no logging [apps/mobile/src/app/(tabs)/history/index.tsx, apps/mobile/src/app/plan.tsx] — zero `console.error`/`console.warn` usage exists anywhere in `apps/mobile/src/app/`; a pre-existing, app-wide convention.
- Negative/anomalous check-in durations silently clamped to zero [apps/mobile/src/app/(tabs)/history/index.tsx:37-40] — identical `Math.max(0, ...)` arithmetic to the dashboard's `AttendancePageClient.formatDuration`, which this story's Dev Notes explicitly instruct mirroring.
- Unlocalized, unformatted currency/price display (`"{{price}} {{currency}}"`) [apps/mobile/src/app/plan.tsx] — explicitly instructed by the story to match `onboarding/plan.tsx`'s identical pre-existing formatting.
- Deactivated/removed member gets a generic, non-recoverable retry loop [apps/mobile/src/app/plan.tsx, apps/mobile/src/app/(tabs)/history/index.tsx] — identical pre-existing member-resolution pattern in `(tabs)/index.tsx` and `profile.tsx`.
- Rapid double-tap can stack duplicate `/plan` modal screens [apps/mobile/src/app/(tabs)/index.tsx handleViewPlan call sites] — no navigation `Pressable` anywhere in this app currently guards against double-tap; a pre-existing, app-wide gap.
- Retry buttons have no in-flight guard, allowing a stale response to overwrite fresher data ["Try Again" buttons in apps/mobile/src/app/plan.tsx, apps/mobile/src/app/(tabs)/history/index.tsx] — identical pre-existing pattern in `onboarding/plan.tsx` and `profile.tsx`.

## Deferred from: code review of story-3-9-member-app-offline-check-in-queueing (2026-07-29)

- `syncPendingCheckIns` deletes queued check-ins on any non-"already open" RPC error, including transient failures [apps/mobile/src/services/checkin.ts] — user decision: leave as-is. A transient server-side error (statement timeout, deadlock, temporary outage) is returned as a normal `{error}` response, not a thrown exception, so it falls into the "delete anyway" bucket even though it may have succeeded on retry; accepted as rare/low-impact at pilot scale rather than adding a maintained permanent-error whitelist.
- No de-duplication for repeated offline scans within the same outage [apps/mobile/src/app/(tabs)/checkin.tsx] — user decision: leave as-is. A second re-scan queues a second `client_scan_id`; the second legitimately hits "already has an open check-in" on sync and stays queued until checkout/timeout, then replays into a second, spurious attendance event for one physical visit — rare and low-stakes (attendance data, not billing).
- Migration 0028's `create unique index` on `attendance_events` is not built `CONCURRENTLY` [supabase/migrations/0028_member_app_offline_check_in_queueing.sql] — user decision: leave as-is. Takes an ACCESS EXCLUSIVE lock for the build duration; matches this repo's own established precedent (`idx_gyms_name_unique`, story 1-5, deferred for the identical reason at identical pilot scale).
- Sync-outcome branching depends on a hardcoded substring match on the Postgres exception message [apps/mobile/src/services/checkin.ts] — pre-existing design choice specified by Task 4/Scope Note #4; fragile if the server message wording ever changes, but not a regression introduced by this diff.
- Pending-sync banner text isn't pluralization-aware for `pendingCount > 1` [apps/mobile/src/locales/en.json, fr.json] — text is copied verbatim from EXPERIENCE.md per Task 9's explicit instruction; a design-level wording change, not a code defect.
- Device clock is trusted for `checked_in_at`; only future skew is clamped, not past skew [supabase/migrations/0028_member_app_offline_check_in_queueing.sql] — this is Scope Note #2's own disambiguation of what gets clamped; not an implementation gap introduced by this diff.
- No accessibility live-region announcement for the offline banner / "Syncing…" text [apps/mobile/src/app/(tabs)/checkin.tsx, index.tsx] — consistent with the rest of this app's existing lack of accessibility props; a pre-existing gap, not unique to this change.
- `client_scan_id` collision with a different member's row would surface as a raw unique-violation and get silently deleted from the queue on next sync [supabase/migrations/0028_member_app_offline_check_in_queueing.sql] — extremely low likelihood given UUIDv4 generation via `Crypto.randomUUID()`, but a real unhandled path worth a follow-up.

## Deferred from: code review of story-3-8-member-app-check-in-result-states (2026-07-29)

- No secondary tie-break (e.g. `id`) if two subscription rows share an identical `created_at` [supabase/migrations/0027_member_app_check_in_result_states.sql:457] — negligible practical risk given microsecond-precision timestamps; same class of gap already deferred from story-3-2's `renew_subscription()` "most recent subscription" lookup, now duplicated in `check_in()` too.
- Subscription status is read without a lock (`for update`) before check-in completes, leaving a narrow TOCTOU race against the periodic subscription-lifecycle batch job (0021) that could let a check-in through in the same window the job flips a status to `expired` [supabase/migrations/0027_member_app_check_in_result_states.sql:457] — consistent with how the rest of the batch-updated subscription-status system already behaves; no AC requires locking.
- Client detects the `'expired'` check-in outcome via substring-matching the raised exception's message rather than a stable error code [apps/mobile/src/services/checkin.ts:81] — matches this codebase's established convention for `already_checked_in`/`recordCheckIn()`'s other branches (Story 3.4 Scope Note #3); a codebase-wide pattern, not introduced by this story.
- The "most recent subscription row wins" business rule is implemented independently in both SQL (`check_in()`) and TS (Home screen's `loadHome` query, Story 3.7) with no shared source of truth or synchronization test — pre-existing duplication pattern (both sides use the identical `order by created_at desc limit 1` shape), not introduced by this story's design choice alone.

## Deferred from: code review of story-3-7-member-app-home-screen-status-display (2026-07-29)

- `gyms` query has no explicit `.eq('id', ...)` scoping filter, relying entirely on RLS + `.single()` [apps/mobile/src/app/(tabs)/index.tsx loadHome] — identical pattern already used by `apps/mobile/src/app/(tabs)/profile.tsx:78` (Story 2.8); not introduced by this story.
- `isSubscriptionRow`'s `plans` shape check (`typeof === 'object'`) would also pass for an array [apps/mobile/src/app/(tabs)/index.tsx isSubscriptionRow] — copied verbatim from `onboarding/plan.tsx`'s existing `isSubscriptionRow`, not introduced by this story.
- No unmount/cancellation guard on the async `loadHome` load [apps/mobile/src/app/(tabs)/index.tsx loadHome] — no screen in this codebase guards against this either.
- Silent, unlogged failure paths in `getRecentCheckIns`/`getOccupancyBand` [apps/mobile/src/services/checkin.ts, apps/mobile/src/services/occupancy.ts] — matches the spec's own best-effort/non-blocking design (Scope Note #2) and no logging/telemetry convention exists anywhere else in this app.
- New `create policy "member_read_own_attendance_events"` migration has no `drop policy if exists` guard or down-migration [supabase/migrations/0026_member_app_home_screen_status_display.sql] — matches every other migration in this repo (verified: none use `drop policy if exists`).
- No `onError` fallback for broken/expired gym-logo or avatar image URLs [apps/mobile/src/app/(tabs)/index.tsx header render] — no existing screen handles this either.

## Deferred from: code review of story-3-6-occupancy-display-admin-attendance-page (2026-07-28)

- `escapeIlike()` (`apps/dashboard/services/attendance.ts`) doesn't escape PostgREST composite-filter metacharacters (comma/parens) in the Daily Log's member-name search. Copied verbatim from `members.ts`'s own `escapeIlike` — if this is actually exploitable, it predates this story and affects the Members search box too, not something introduced here.
- Each of `getCurrentlyCheckedIn()`/`getTodayAttendanceCount()`/`listAttendanceLog()` independently resolves `getCallerGymId()`/`supabase.auth.getClaims()` rather than once per page load. Matches every other service file's established per-function convention in this codebase; `getClaims()` is a local JWT decode, not a network round trip, so the redundancy is real but low-cost.
- `/attendance`'s page component (`apps/dashboard/app/(dashboard)/attendance/page.tsx`) blanks the whole page on any single failed sub-request in its `Promise.all`, discarding data from calls that succeeded. Verified this exactly mirrors `members/page.tsx`'s own established all-or-nothing error-check pattern — not a new issue introduced by this story.
- `mapSupabaseError`'s new branches for `check_out_member()`'s raises (`packages/types/src/errors.ts`) match on raw message substrings, brittle against future SQL wording changes with no compiler/test signal. This is the file's established, spec-mandated pattern (Story 3.6's own Scope Note #6 explicitly directs following `renew_subscription()`'s exact substring-match style) — a systemic file-wide characteristic, not unique to this story.
- Deactivated-but-still-checked-in members are only cosmetically badge-flagged on the Attendance page's Currently Checked-In table, with no forced-checkout action or explanation for how a deactivated member ended up with an open session. A real product question (compliance-relevant: a revoked member still "inside" per the system), but never mentioned in Story 3.6's AC or Scope Notes — needs a follow-up story/decision if it matters.
- `formatDuration()` (`AttendancePageClient.tsx`) silently clamps a negative duration to `"0h 0m"` instead of surfacing the anomaly (checkout timestamp earlier than check-in — clock skew or direct DB manipulation). Low likelihood, and the right handling (log a warning? distinct display?) isn't specified.
- The Daily Log's empty-state copy ("No check-ins recorded for this period") is identical whether the date range genuinely had zero check-ins or the member-name search simply matched nothing — a staff member who mistypes a name gets no signal it's their search, not the day, that's empty. Polish, not specified by the story.

## Deferred from: dev of story-3-6-occupancy-display-admin-attendance-page (2026-07-25)

- FR-047's "the raw count and capacity must never reach the member client, in any form, ever" is fully closed for the checked-in *count* (attendance_events has zero SELECT policies for `member`, verified via this story's own pgTAP suite) but **not** for `gyms.capacity` itself — the pre-existing "read own gym" RLS policy [supabase/migrations/0009_auth_hook_gym_claims.sql:154-156] has no role gate at all (`using (id = private.gym_id())`), so any authenticated session with a `gym_id` claim — including `app_role = 'member'` — can already `select capacity from gyms where id = <own gym>` directly via PostgREST and get the real value back. Verified hands-on against the local instance (via `docker exec ... psql`, inside WSL): a member-claim session's direct `select capacity from gyms ...` returned the real value (42), not 0 rows. This predates this story (the policy shipped in Story 1.8) and this story's own Project Structure Notes explicitly forbid touching any existing migration file, so it isn't fixed here. `member_occupancy_band()` itself never returns capacity/count in its own output (only the band label) — the gap is a second, independent access path (raw table access) this story's own security requirement doesn't close. Worth a follow-up story/decision on whether `gyms`' SELECT policy should split into a role-gated "full row" policy (staff) and a narrower member-visible subset (or a `security_invoker` view exposing only non-sensitive columns).
- Task 8's "manually verify `/attendance` renders for a seeded owner/manager/receptionist session" could not be completed via a live browser this session. **Updated diagnosis (2026-07-28, re-investigated in a follow-up session):** the original theory below (a Windows↔WSL loopback-forwarding/firewall gap) was wrong, or at least not the proximate cause. `wsl --shutdown` + restart DID initially restore Windows-native reachability to `127.0.0.1:54321` (confirmed repeatedly via PowerShell `Invoke-WebRequest` and `curl.exe`, HTTP 200) — but connectivity then became intermittent again within minutes, including for plain `curl.exe`/PowerShell with no Node involved. Root-caused via `journalctl -u docker` inside WSL: **`docker.service` (dockerd) itself is crash-looping on this machine**, restarting roughly every 30–90 seconds (`systemctl status docker` repeatedly shows `Active: active (running) since <a few seconds ago>`; the journal shows a tight cycle of "Removing stale sandbox" / "layer not mounted" / "Deleting nftables rules" errors on every restart, i.e. unclean shutdowns, not graceful stops). Each dockerd restart tears down and rejoins every container's network endpoint, which cascades: `supabase_db_gym_os` restarts mid-cycle → `supabase_auth_gym_os` (GoTrue) fails its migration step with "the database system is starting up" / "connection refused" and crash-loops → Kong never reaches `healthy` → the Supabase stack is reachable only in the brief windows between dockerd restarts. This fully explains the earlier session's inconsistent results (curl sometimes succeeding, Node sometimes failing, "flaky" reachability generally) — it was never about *which Windows process* was connecting, it was about *which second* it connected in, relative to dockerd's crash cycle. Confirmed independent of this session's own `wsl -e` invocations via a passive 2-minute `curl.exe`-only poll (every 15s) that also came back 000 the entire time. **Not yet root-caused further**: `free -h` showed 6.8GB free (not OOM; `dmesg` had no OOM-killer hits), and no explicit `systemd` "Failed with result" line was found in the journal — why dockerd itself keeps dying (vs. being killed) is still open. Whoever picks this up next should check: Windows Update/Hyper-V changes around this timeframe, antivirus/EDR interference with `dockerd`/`containerd`, disk health under the WSL2 vhdx, or a corrupt Docker Engine install inside the distro (worth trying `sudo apt reinstall docker-ce docker-ce-cli containerd.io` or recreating the WSL distro as a last resort). AC/behavior coverage was instead verified via the real pgTAP suite (executed inside WSL against a genuine local Postgres instance, in the brief windows dockerd stayed up) plus `tsc`/`eslint`/i18n-parity — thorough for backend logic and RLS, but the actual React/Next.js page rendering was never exercised in a live browser, in either session.

  <details><summary>Original (2026-07-25) diagnosis, superseded above</summary>

  Windows-native processes (curl, Node, and the `next dev` server itself) cannot reach the WSL2-hosted Supabase Docker stack on `127.0.0.1:54321`/`:54322`, even though `.wslconfig` has `networkingMode=mirrored` and a full `wsl --shutdown` + restart was tried. `curl`/`docker exec psql` *from inside* WSL reach the stack fine (confirmed: `supabase test db`'s 318/318 pass, and the `gyms.capacity` finding above was verified this way), so this is a Windows↔WSL localhost-forwarding gap specific to this machine/session, not a code defect. Whoever next needs a live dashboard browser session against local Supabase should diagnose why mirrored-mode loopback forwarding isn't working here (Windows Defender Firewall profile on the "vEthernet (WSL)" adapter is a likely first suspect) before assuming it's fixed from the earlier mobile/phone-testing fix (that one was about phone-on-LAN reaching WSL's shared IP, not Windows-native-process-to-127.0.0.1).

  </details>

## Deferred from: code review of story-3-5-check-out-manual-auto-timeout (2026-07-24)

- Race between manual check-out and the 15-minute auto-timeout cron job [supabase/migrations/0024_check_out_manual_auto_timeout.sql:44-50,118-125,171-178] — if the cron job closes a session in the same window a manual check-out targets it, the manual call raises the generic "has no open check-in" error instead of a distinguishable "already closed" outcome. Low-probability window; matches architecture.md's documented "no advisory lock, no automatic retry" convention already established by `renew_subscription()` (0022) and `check_in()` (0023).
- No client-side min/max/step bounds on the new `checkinTimeoutHours` number input [apps/dashboard/app/(dashboard)/settings/SettingsForm.tsx:387-394] — the existing `gracePeriodDays`/`capacity`/`alertAutoDismissMinutes` fields follow the identical pattern of relying solely on Zod validation after submit, with no `min`/`max`/`step` attributes on the `<Input>` itself.
- No DB-level upper-bound constraint on `checkin_timeout_hours` [supabase/migrations/0023_member_check_in_one_open_session_enforcement.sql:28-29] — the column's DB constraint is `check (checkin_timeout_hours > 0)` only; the 1-24 upper bound is Zod-only, matching `gracePeriodDays`/`alertAutoDismissMinutes`'s identical app-layer-only upper-bound convention.
- `checkOutMember()` discards the RPC's returned `attendance_events` row, returning only `{ error }` [apps/dashboard/services/attendance.ts:14-20] — matches Scope Note #4's code block exactly as specified; worth reconsidering once Story 3.6 builds the consuming "Check Out" button UI and needs the checkout timestamp to display.
- No test coverage for `apps/dashboard/services/attendance.ts`'s `checkOutMember()` — no `.test.ts` files exist anywhere under `apps/dashboard/services/` today; codebase-wide gap, not specific to this story.
- Non-integer input (e.g. "1.5") into `checkinTimeoutHours` surfaces Zod's raw "expected int, received float" message instead of the tailored range copy [apps/dashboard/app/(dashboard)/settings/SettingsForm.tsx:190-198] — the NaN-substitution loop only catches `Number.isNaN`, not int-vs-float mismatches; `gracePeriodDays`, `capacity`, and `alertAutoDismissMinutes` share the identical gap.

## Deferred from: code review of story-3-4-member-check-in-one-open-session-enforcement (2026-07-24)

- Client-side error classification relies on substring-matching the Postgres error message/constraint name [apps/mobile/src/services/checkin.ts:36] — fragile in principle, but this is the literal pattern Scope Note #3 prescribes and mirrors `validateGymToken()`'s established shape from Story 3.3; not a new deviation.
- New Success/Already-Checked-In overlays have no accessibility live-region announcement for screen readers [apps/mobile/src/app/(tabs)/checkin.tsx:268] — pre-existing gap in the Wrong-QR/network-error overlays this story extends, not a new regression.
- `recordCheckIn()`'s catch-all and generic error branch have no logging/telemetry [apps/mobile/src/services/checkin.ts:41] — matches `validateGymToken()`'s existing silent-catch pattern exactly.
- No unit/component test coverage for the new client-side branching logic in `checkin.ts`/`checkin.tsx` [apps/mobile/src/app/(tabs)/checkin.tsx] — this repo has no existing precedent for mobile unit tests; the established convention is pgTAP + manual on-device verification, which this story followed.
- The new partial unique index (`idx_attendance_events_one_open_per_member`) will fail the migration outright if pre-existing duplicate-open-session data violates it [supabase/migrations/0023_member_check_in_one_open_session_enforcement.sql:20] — operational/deployment risk, not a code-level fix; no known conflicting data exists today since no prior story wrote to `attendance_events` via a real check-in flow.

## Deferred from: code review of story-3-2-manual-renewal-reset (2026-07-18)

- Nondeterministic "most recent subscription" tie-break on identical `created_at` [supabase/migrations/0022_manual_renewal_reset.sql, apps/dashboard/services/members.ts] — both `renew_subscription()`'s plan_id lookup and `deactivateMember`'s new lookup order solely by `created_at` with no secondary tie-breaker; mirrors an already-established "most recent" read pattern elsewhere in `members.ts`. Real but extremely low-probability in production since each renewal is its own transaction with microsecond-resolution timestamps — the story's own pgTAP suite had to deliberately backdate fixture rows to avoid tying within a single frozen-`now()` test transaction (a test-harness-only concern, per the story's own Debug Log).
- `deactivateMember`'s subscription UPDATE assumes the looked-up row still exists [apps/dashboard/services/members.ts:686-720] — no current code path deletes `subscriptions` rows, so the SELECT-then-UPDATE-by-id sequence's implicit assumption that the row found by the SELECT still exists at UPDATE time isn't reachable today; worth revisiting if a future story ever adds subscription deletion.
- Race condition between `deactivateMember`'s SELECT-then-UPDATE and a concurrent `renew_subscription()` call [apps/dashboard/services/members.ts:698-720] — low-probability, no AC requires it; requires two staff acting on the same member within a sub-second window, and matches this story's own accepted no-concurrency-guard precedent. `deactivateMember` reads the member's most-recent subscription id, then updates that specific row to `expired` in a separate statement; if `renew_subscription()` inserts a newer `active` row for the same member in between, the UPDATE still succeeds against the now-stale id, leaving the member `deactivated_at` set while their actual most-recent subscription row remains `active`.
- Two different "member not found" error codes/copy now exist [apps/dashboard/services/members.ts, packages/types/src/errors.ts] — different layers, not one inconsistent call site; local service-guard helper vs. shared RPC-exception mapper are legitimately separate, no single call site produces conflicting output today. The pre-existing `memberNotFoundError()` helper returns `code: "not_found"` with `members.errors.memberNotFound`, while this story's new `mapSupabaseError` mapping for `renew_subscription()`'s raise returns `code: "member_not_found"` with the new shared `errors.memberNotFound` key.

## Deferred from: code review of story-3-1-subscription-lifecycle-cron-job (2026-07-18)

- No path back to `active` after a renewal while a subscription sits in `expiring_soon`/`grace_period` [supabase/migrations/0021_subscription_lifecycle_cron.sql run_subscription_lifecycle_job] — by design: this story's three `UPDATE`s are forward-only decay; Epic 4 Story 4.8 ("Subscriptions page + manual renewal") owns resetting a subscription back to `active`/pushing `expiry_date` forward on a real renewal, per Scope Note #1.
- Double-fault inside the exception handler's own recovery statements isn't further guarded [supabase/migrations/0021_subscription_lifecycle_cron.sql run_subscription_lifecycle_job] — if the `job_runs`/`log_audit_event` writes *inside* the `EXCEPTION WHEN OTHERS` block themselves raise (e.g. an unexpected `audit_log` failure), that second exception propagates uncaught and rolls back the failure record too. Accepted as a low-probability risk: `log_audit_event()` deliberately doesn't swallow exceptions by its own documented design (0007_audit_log.sql), and a second nested savepoint layer for this narrow double-fault case isn't currently justified.
- No advisory-lock/concurrency guard against a manual verification call racing the real scheduled run [supabase/migrations/0021_subscription_lifecycle_cron.sql] — mitigated in practice: the job's absolute-date `UPDATE`s are idempotent (a concurrent/duplicate run just no-ops on already-transitioned rows), worst case is a duplicate `job_runs` success row for one night. No AC requires locking.
- `gyms.timezone` is joined in step 1 (for `grace_period_days`) but unused in the date math — every transition compares against a single global `current_date` [supabase/migrations/0021_subscription_lifecycle_cron.sql] — already a deliberate, documented story-level simplification (this story's own Dev Notes: fixed WAT arithmetic, no `timezone()` conversion). All four of the schema's WAT-equivalent African zone options (Douala/Lagos/Bangui/Kinshasa) share the same UTC+1, no-DST offset; only a gym explicitly configured to plain `UTC` would see any discrepancy.
- `gyms.grace_period_days` has no DB-level CHECK constraint, only the dashboard's Zod 1–30 bound (`packages/types/src/schemas/gym.ts`) [supabase/migrations/0002_gyms_and_tiers.sql] — pre-existing gap since the column's introduction (Story 1.2), not introduced by this story; this story just newly makes the column load-bearing in `run_subscription_lifecycle_job()`'s date arithmetic, where a `0`/negative value (if ever written outside the app-layer form) would collapse the `grace_period` state.

## Deferred from: code review of story-2-8-member-self-service-profile-management (2026-07-18)

- Storage upload happens before the DB write is confirmed; on a DB-write failure the storage object at the same path is already overwritten with no compensating rollback [apps/mobile/src/lib/photo-upload.ts uploadPhoto] — inherited verbatim from `onboarding/profile.tsx`'s original implementation (predates this story), now duplicated at a second call site (`(tabs)/profile.tsx`'s `handleSaveProfile`) via the Task 1 extraction; not introduced by this story.
- `asset.fileSize` undefined skips the client-side size guard entirely (e.g. on web or with some pickers) [apps/mobile/src/lib/photo-upload.ts pickPhoto] — lifted verbatim from the original onboarding photo-picker logic, not introduced by this story.
- Unmapped photo extension (e.g. heic/heif from an iOS camera capture) silently defaults Content-Type to `image/jpeg` [apps/mobile/src/lib/photo-upload.ts EXTENSION_TO_MIME/uploadPhoto] — lifted verbatim, not introduced by this story.
- Zero automated test coverage for the new `photo-upload.ts` module or the new `(tabs)/profile.tsx` screen [apps/mobile/src/lib/photo-upload.ts, apps/mobile/src/app/(tabs)/profile.tsx] — matches the existing project-wide precedent of no mobile unit tests (already a logged gap in Stories 2.6/2.7); no test runner is wired for `apps/mobile` yet.

## Deferred from: code review of story-2-6-member-app-phone-otp-onboarding-through-profile-setup (2026-07-17)

- `phone_has_membership` has no rate limit of its own, callable directly via PostgREST at unlimited volume to enumerate registered phone numbers, bypassing GoTrue's per-IP `/otp` throttle entirely [supabase/migrations/0019_member_onboarding_otp.sql:31] — already an accepted, documented risk (Scope Note #3 of this story, `docs/decisions.md` Decision 3): narrows but does not eliminate the enumeration/abuse surface; CAPTCHA remains explicitly out of scope until a client-side widget exists.
- `otp_resend_attempts` has no retention/cleanup mechanism — rows accumulate indefinitely for every phone number that ever requests a resend, including abandoned/attacker-probed numbers [supabase/migrations/0019_member_onboarding_otp.sql] — real gap, but low severity at pilot scale (NFR-009); building retention (e.g. a `pg_cron` sweep) is new scope requiring a separate ops decision on retention policy, not an unambiguous patch.
- Resend's `signInWithOtp` call in `otp.tsx`'s `handleResend()` isn't re-gated by a fresh `phone_has_membership` check before sending [apps/mobile/src/app/onboarding/otp.tsx:119] — a literal gap against Scope Note #3's "gate every `signInWithOtp` call" reading, but unreachable in practice: `phone` context state is only ever set in `phone.tsx` immediately after a successful `phone_has_membership` check, and the sequencing guard blocks reaching `/onboarding/otp` with no phone set.

(No DB-level constraint on the newly self-writable `users.display_name`/`users.photo_url` columns beyond RLS row-ownership was also flagged, but is not a new gap — it matches this project's already-documented "Zod is the single source of validation, no DB CHECK constraints yet" precedent, see lines 90 and 103 below.)

## Deferred from: dev of story-2-6-member-app-phone-otp-onboarding-through-profile-setup (2026-07-17)

- `send-sms-hook/index.ts` still hardcodes the OTP delivery locale to `"en"` (line 125) regardless of the member's MA-01 language selection. Investigated hands-on (Task 10) whether passing `options: { data: { locale } }` to `signInWithOtp` propagates into an *existing*, admin-pre-created user's `raw_user_meta_data`: inconclusive this session — the local Send SMS Hook consistently returned `hook_timeout` (5s) because the Edge Functions runtime isn't served by plain `supabase start` in this environment (confirmed: no `supabase_edge_runtime_gym_os` container was up), so the `/auth/v1/otp` request never completed far enough to isolate GoTrue's own metadata-merge behavior from the hook-timeout's effect on the transaction. Whoever revisits this should first get `send-sms-hook` actually reachable locally (`supabase functions serve` in a separate terminal, or diagnose why the edge-runtime container doesn't stay up under `supabase start` alone) before re-attempting the test. English-only OTP copy is a real but minor degradation — no AC in Story 2.6 requires localized OTP delivery.
- `apps/mobile`'s own `expo lint` still cannot run at all — `Error: Cannot find module 'eslint'` (pre-existing gap, `.github/workflows/ci.yml`'s lint step already excludes `apps/mobile` for this exact reason). This is more consequential after Story 2.6 than before: `apps/mobile` now has ~15 real source files with real user-facing strings and no `i18next/no-literal-string`-equivalent CI gate enforcing FR-016 against them (the dashboards have had this gate since Story 1.10). Fixing the toolchain gap (installing/configuring `eslint` for the Expo/React Native ecosystem, then wiring an equivalent literal-string rule) is real, standalone scope, not attempted as part of this story.
- MA-02's country-code selector is a fixed `+237` (Cameroon-only) prefix, not EXPERIENCE.md's full tappable bottom-sheet with a searchable country list [apps/mobile/src/app/onboarding/phone.tsx]. Deliberate scope reduction: the pilot and NFR-009 are Cameroon-only, and no FR requires multi-country support in V1. Revisit if the platform ever expands beyond Cameroon.
- MA-05's photo picker uses a native `Alert.alert` with three buttons (Take Photo / Choose from Library / Cancel) as the "action sheet," not a true bottom-sheet component [apps/mobile/src/app/onboarding/profile.tsx] — functionally equivalent, visually a dialog rather than a sheet on iOS. No extra dependency was added to build a real bottom sheet; revisit if design review flags the visual mismatch.
- `member-photos` Storage bucket is `public = true` (mirrors the `gym-logos` precedent, Story 1.9 Decision 3), even though — unlike gym-logos, which genuinely needs unauthenticated reads for the 24h branding cache — no current screen reads a member's own photo unauthenticated. Chosen for consistency with the only proven bucket pattern in this codebase rather than introducing a first signed-URL pattern; revisit if a future story's data-exposure review flags public member-photo URLs as a concern.
- `users.display_name`/`users.photo_url` are now member-self-writable and used as the "new account" signal (`display_name IS NULL`) for whether MA-05 (Profile Setup) is shown. This is this story's own synthesized design decision (no planning doc defines "new account"), not a documented product decision — recorded in the story's Scope Note #2; worth a `docs/decisions.md` entry once Story 2.8 (Member Self-Service Profile Management) confirms it builds on the same fields.
- No manual QA on a physical device or simulator was performed for the MA-01→MA-05 flow this session (no device/emulator available in this environment) — verified instead via direct RPC calls against the real local Supabase instance (migration, RLS, resend/lockout logic all hands-on tested) and `tsc`/i18n-parity checks. A real device/Expo Go walkthrough is still needed before this story can be considered fully QA'd per `architecture.md`'s "manually QA'd on a physical Android device" standard.

## Deferred from: code review of story-2-4-csv-member-import (2026-07-17, cross-story diff review)

- `confirmCsvImport` processes up to `IMPORT_ROW_LIMIT = 1000` rows fully sequentially inside one Server Action invocation — on a platform-imposed function timeout, the process would be killed mid-loop with no JS-level compensating-cleanup/rollback ever running, leaving a partially-imported, un-rolled-back batch (contradicts AC #2/#3's "all-or-nothing" guarantee for a large-enough import) [apps/dashboard/services/csvImport.ts confirmCsvImport] — accepted at pilot scale, matches Scope Note #3's already-recorded "disproportionate effort at pilot scale" reasoning (NFR-009, ~30 members/gym); revisit if real-world imports approach the 1000-row ceiling.

## Deferred from: code review of story-2-4-csv-member-import (2026-07-17)

- `deactivateMember` surfaces a raw, hardcoded-English Zod message directly to the user, contradicting `createMember`'s own "never surface raw Zod messages to a French-locale user" discipline in the same file [apps/dashboard/app/(dashboard)/members/actions.ts:606-611] — pre-existing, inherited from Story 2.3, not touched by this story's declared changes.
- `provisionMemberRow`'s `findOrCreateUserByPhone` step (creates a real `auth.users` account) runs before any role/authorization check — RLS only blocks the later `insertMember` call [apps/dashboard/services/members.ts provisionMemberRow] — pre-existing, inherited verbatim from Story 2.3's `createMember` orchestration via the Task 4 extraction, no behavior change.
- Stale/out-of-range `page` query param renders a misleading "no members match your search or filter" message when no search/filter is actually active [apps/dashboard/services/members.ts listMembers pagination] — pre-existing Story 2.3 pagination logic, unrelated to this story's CSV import changes.
- Non-UTF-8 file encodings (e.g. Windows-1252 exports with accented names) are silently mis-decoded by `FileReader.readAsText()` with no charset detection [apps/dashboard/app/(dashboard)/members/components/CsvImportModal.tsx readFileAsText] — low priority at current pilot scale (NFR-009).
- Unclosed/unbalanced quote characters cause the hand-rolled parser to silently merge subsequent rows/fields into one giant field with no parse error [apps/dashboard/lib/csv.ts] — accepted limitation of the hand-rolled RFC4180-subset parser (Scope Note #4).
- Downloaded CSV template is header-only with no example data row, making first-attempt formatting mistakes more likely for non-technical users [apps/dashboard/app/(dashboard)/members/components/CsvImportModal.tsx downloadTemplate] — UX enhancement not required by any AC.

## Deferred from: code review (Round 2) of story-2-3-manager-owner-create-edit-deactivate-members (2026-07-17)

- `deactivateMember`'s subscription UPDATE filters only by `gym_id`+`member_id`, not scoped to the member's current subscription — would force-expire every subscription row for a member who ever has more than one [apps/dashboard/services/members.ts deactivateMember] — unreachable under this story's own single-subscription data model (a member has at most one subscription row today); revisit when Story 3.2's renewal work introduces subscription history/multiple rows per member.

## Deferred from: code review (Round 2) of story-2-2-membership-plan-configuration (2026-07-16)

- `logPlanChange` failure is misattributed to "audit log failed to write" even when the real cause is a `getCallerGymId` claims error inside `logPlanChange` itself, not the audit RPC [apps/dashboard/services/plans.ts] — extremely low practical reachability (the caller already resolved `getCallerGymId` successfully earlier in the same request), same low-severity "attribution" class as the already-accepted `getCallerGymId`/not-found conflation.
- `planNameExists` silently swallows a `getCallerGymId` error and reports "name not taken" instead of surfacing it [apps/dashboard/services/plans.ts] — benign in practice: the immediately-following `insertPlan`/`updatePlan` call independently re-resolves `getCallerGymId` and correctly surfaces the same error moments later.
- No optimistic concurrency for concurrent plan edits (last-write-wins if two managers edit the same plan simultaneously) [apps/dashboard/services/plans.ts updatePlan] — matches story 1-6's own accepted precedent for this exact class of gap at this platform's "1-3 gyms" pilot scale (NFR-009); a real fix needs an atomic versioned UPDATE.

## Deferred from: review-fixes pass on story-2-2-membership-plan-configuration (2026-07-16)

- `pnpm typecheck` currently fails on `apps/dashboard/app/layout.tsx(46,8)` — `next-themes`'s `ThemeProviderProps` type doesn't accept a `children` prop under the currently-resolved React 19 types. Confirmed pre-existing and unrelated to story 2.2 (`git log -1 -- apps/dashboard/app/layout.tsx` shows the file untouched since commit `e0e538d`, predating this story; zero working-tree diff). Not a stale `.next` type-cache artifact either — reproduces on a clean `tsc` run after deleting `apps/dashboard/.next`. Root cause: `pnpm-lock.yaml` resolves two different `next-themes@0.4.6(react-dom@…)(react@…)` variants (`19.2.3` and `19.2.7`) in this monorepo. This contradicts an earlier session's own recorded claim that `pnpm typecheck` passed clean for `@gymos/dashboard` — the environment appears to have drifted (a dependency install) between sessions. Needs whoever next touches `apps/dashboard/app/layout.tsx` or the React/next-themes dependency graph to resolve the duplicate resolution (e.g. a `pnpm.overrides` pin) and re-verify. **Update (2026-07-17, Story 2.6 dev session):** this specific error is gone after a from-scratch `pnpm install` (`.pnpm` virtual store fully rebuilt as part of recovering from an unrelated Windows file-locking issue caused by `apps/mobile`'s `@supabase/supabase-js: "latest"` — since fixed to a pinned version, `apps/mobile/package.json`). `pnpm run typecheck` now reports 0 errors across all 4 packages (dashboard, super-admin, types, mobile — including mobile's own previously-tracked `Image`/`NativeTabs` type errors below). Plausibly the duplicate `next-themes` resolution this entry describes got deduplicated by the clean reinstall, but this was a side effect of unrelated recovery work, not a deliberate fix — re-verify on the next `pnpm install` in case it regresses again.

## Deferred from: code review of story-2-2-membership-plan-configuration (2026-07-16)

- Audit log write failures on plan CRUD are swallowed as a soft toast with no retry [apps/dashboard/services/plans.ts] — pre-existing pattern shared with tiers/gym-settings, not new to this story.
- `getCallerGymId` conflates "no `gym_id` claim" (an auth anomaly) with a generic not-found error [apps/dashboard/services/plans.ts] — copied verbatim from gym-settings.ts's existing pattern, not new to this story.
- UPDATE/DELETE blocked by RLS returns the same "not found" message as a stale/cross-gym id, rather than a distinct permission-denied message [apps/dashboard/services/plans.ts] — exact pre-existing pattern copied from gym-settings.ts's `gymNotFoundError`, not introduced by this story.
- `plans.errors.planInUse` has no i18next plural (`_one`/`_other`) suffix — renders "1 subscriptions use this plan" for count = 1, in both locales [apps/dashboard/locales/en.json, apps/dashboard/locales/fr.json] — same already-accepted project-wide gap as story 1-10's review; fix as part of a dedicated pluralization pass across the whole app, not per-string.

## Deferred from: code review of story-1-11-gym-owner-activation-temp-password-sms (2026-07-16)

- No unit test coverage for `generateTempPassword()`'s rejection-sampling loop (alphabet exclusion, length, termination) [apps/super-admin/app/(admin)/gyms/actions.ts] — real gap, but no confirmed JS/TS unit-test runner in this app yet; not blocking this story.
- No rate-limiting/idempotency guard against duplicate `createGym` submissions causing repeated billed WhatsApp sends [apps/super-admin/app/(admin)/gyms/actions.ts] — same category of gap already tracked below for the SMS hook's provider selection (no automatic fallback/retry accounting); pre-existing double-submit risk class, now with a real per-message cost.
- Trigger's per-column allow-list pattern (`0016_owner_must_change_password.sql`) has no structural safeguard against a future developer forgetting to extend it for a new self-writable `users` column [supabase/migrations/0016_owner_must_change_password.sql] — valid future architectural improvement (e.g. inverting to a checked deny-list), out of this story's scope, consistent with the pattern established since Story 1.10.

## Deferred from: Story 1.11 (Gym Owner Activation via Temp Password), dev implementation (2026-07-16)

- `apps/super-admin/lib/messaging/sendTempPasswordMessage.ts` has no SMS fallback and no automatic retry on a second channel -- WhatsApp-via-`verifications_2fa_template` is the sole delivery path for the temp-password message (Open Question 1, resolved; see `docs/decisions.md`'s 2026-07-16 entry). A failed send is non-blocking (gym/owner/member creation still succeeds, AC #7) and the temp password is always surfaced in the Super Admin UI as a manual fallback, but no automatic SMS retry exists. Same category of gap as `send-sms-hook/index.ts`'s single-provider `OTP_PROVIDER` selection, below -- revisit together if a real pilot failure makes single-channel delivery a practical problem.

## Deferred from: party-mode decision on onboarding/account-recovery channel policy (2026-07-15)

- Email-based onboarding invitation links and email-based password-reset — a genuinely new future channel, not a variant of the current SMS/WhatsApp `OtpDeliveryProvider` abstraction. Needs its own scoping pass: an `EmailOtpProvider`-equivalent (SMTP/Resend, not Twilio/sent.dm's WhatsApp Business template pipeline — no Meta approval wall applies to email), a link/token state machine (generation, expiry, single-use), and — before any of that — confirmation that member/owner email addresses are actually collected anywhere in the current onboarding flow (flagged by Sally during the party-mode discussion; if no email is on file for existing users, the channel has no audience to ship to). Deferred until the WhatsApp/SMS OTP mechanism (Stories 1.11, 2.5, 2.6) has shipped and proven out in the pilot — see `docs/decisions.md`'s 2026-07-15 "Onboarding/account-recovery channel policy" entry.
- No automatic WhatsApp→SMS runtime fallback exists in `send-sms-hook/index.ts`'s `getProvider()` [supabase/functions/send-sms-hook/index.ts:25-36] — it selects exactly one provider from a single `OTP_PROVIDER` env var. The 2026-07-15 decisions.md entry's "WhatsApp default, SMS follows" is operational config guidance (which value to set per environment), not a claim that cascading in-code fallback logic exists. Building real automatic fallback (retry on a second provider if the first's send fails) is new scope, not assumed by that decision — revisit if a real pilot failure makes single-provider selection a practical problem.

## Deferred from: code review of story-2-1-sms-otp-provider-sandbox-spike, second pass (2026-07-15)

- Module-scope hoisting caches the webhook secret and provider selection for the isolate's lifetime [supabase/functions/send-sms-hook/index.ts] — a rotated `SEND_SMS_HOOK_SECRET` or flipped `OTP_PROVIDER` is silently ignored by already-warm isolates until a redeploy recycles them. Accepted as an operational caveat: secret rotation is rare and deliberate, self-resolved by an intentional redeploy; a per-request freshness check would partially revert the prior round's intentional hoisting-for-perf fix.
- No idempotency guard against hook retries [supabase/functions/send-sms-hook/index.ts] — a GoTrue retry after a lost response could double-send/double-bill the same OTP. Narrow failure window, tiny pilot volume, and proper dedup needs infrastructure (Deno KV or a table) that doesn't exist yet. Revisit with Story 2.6, the first story with real member volume through this hook.
- `enable_signup`/unconfirmed-`public.users`-row risk is more accurately an unauthenticated SMS-pumping/toll-fraud cost-abuse vector (CAPTCHA fully disabled, rate limits are per-IP throttles not hard caps) [supabase/config.toml] — re-characterized in `docs/decisions.md` (was previously understated as DB-hygiene risk). Fix deferred to Story 2.6 because CAPTCHA can't be enabled in isolation without a matching client-side widget, which ships with Story 2.6's real member-facing signup UI.
- `OtpDeliveryProvider.locale` is silently discarded by `SentDmProvider`/`TwilioWhatsAppProvider` (fixed single-language templates) [supabase/functions/send-sms-hook/_shared/otp-providers/{SentDmProvider,TwilioWhatsAppProvider}.ts] — already flagged in this story's own Change Log as Story 2.6's scope, not a defect here.
- Zero automated test coverage for signature verification, payload validation, and provider dispatch [supabase/functions/send-sms-hook/index.ts] — consistent with this story's explicit "spike, not feature build" framing; no test infrastructure exists yet for edge functions in this repo.
- Hook-failure fallback produces a misleading Twilio `20003`/"invalid username" error when the hook is disabled or broken [supabase/config.toml] — already live-tested and documented in `docs/decisions.md` during the prior review round as a known, recognizable-in-incident footgun rather than something to restructure now.
- `SentDmProvider`'s `var_2`/`code` template-parameter coupling is enforced only by a comment, not a runtime guard [supabase/functions/send-sms-hook/_shared/otp-providers/SentDmProvider.ts] — already deliberately addressed in the prior review round via an explicit `templateParameters()` helper + over-communicated comment rather than brittle runtime coupling checks.

## Deferred from: mobile CSS-module ambient-types fix (2026-07-13)

- `apps/mobile/src/constants/theme.ts`'s `import '@/global.css'` is a universal (non-`.web.ts`) file, so this import reaches native (iOS/Android) bundles too, not just web -- untested whether Metro can actually resolve/strip a plain CSS import on native, or whether it'd fail at bundle time. Not addressed here since `apps/mobile` has no runnable code yet (Epic 2+, per Story 1.1's own deferred notes) -- this is a static-typing-only fix (`css.d.ts`), it doesn't touch Metro's runtime bundling behavior. Revisit when the mobile app first actually builds/runs on a native target.
- Local `tsc --noEmit` on Windows does not reproduce CI's `TS2307`/`TS2882` CSS-import errors at all (identical output with or without `css.d.ts` present), and separately surfaces ~15 "Image cannot be used as a JSX component" / `NativeTabsProps` errors that never appeared in CI's log. The two environments disagree on which errors fire for the exact same source -- root cause not identified (suspected `moduleResolution: "bundler"` + platform-specific dependency resolution divergence). Worth investigating if this recurs or blocks a future mobile fix from being locally verifiable.

## Deferred from: code review of spec-cache-components-suspense-boundary-fix (2026-07-13)

- `LocaleShell` (the async component that resolves locale + wraps `ThemeProvider`/`I18nClientProvider`) is duplicated verbatim between `apps/dashboard/app/layout.tsx` and `apps/super-admin/app/layout.tsx` — pre-existing duplication pattern between the two apps' root layouts (they were already near-identical before this fix, no shared root-layout package exists), not introduced by this change; worth extracting to a shared package if/when the two apps' root layouts need to stay in lockstep on more than just this.
- None of the four newly-Suspense-wrapped layouts (`apps/dashboard/app/layout.tsx`, `apps/dashboard/app/(dashboard)/layout.tsx`, `apps/super-admin/app/layout.tsx`, `apps/super-admin/app/(admin)/layout.tsx`) has an accompanying `error.tsx` verified to exist at the right level — if `getRequestLocale()`, `getDashboardShellContext()`, or the admin claims check throws a non-redirect error, there's nothing at this level explicitly confirmed to catch it. Same exposure existed before this change (an unhandled throw in the old fully-async layouts would have hit the same fallback), not a new gap; worth an explicit `error.tsx` coverage audit for both apps' route trees.

## Deferred from: code review of story-1-10-bilingual-en-fr-platform-foundation, UI extraction pass (2026-07-10)

- `updateLanguagePreference`'s "no session at all" error path reuses the generic `common.somethingWentWrong` message instead of a distinct, meaningful message [apps/dashboard/services/session.ts, apps/super-admin/services/gyms.ts] — low priority, the case is effectively unreachable (these pages sit behind auth middleware).
- Translation keys scoped to one UI surface are reused verbatim elsewhere: `gyms.create.statusActive/Suspended/Deactivated` (Create Gym form) also drives the Gym Detail page and Gyms list table/filter; `gyms.lifecycle.reason` is also reused by `EscalateAccessDialog` — the wording happens to be correct in every context today, but this couples otherwise-unrelated UI surfaces to one shared copy choice. Revisit with dedicated keys (e.g. `gyms.status.active`) if any of these surfaces ever needs to diverge.

## Deferred from: code review of story-1-10-bilingual-en-fr-platform-foundation, i18n infra pass (2026-07-10)

- The ESLint `i18next/no-literal-string` gate's default `mode: "jsx-text-only"` never checks JSX attribute values (`aria-label`, `alt`, `title`, etc.) — the `jsx-attributes` config block this story added is currently dead configuration [apps/dashboard/eslint.config.mjs, apps/super-admin/eslint.config.mjs] — switching to `mode: "jsx-only"` closes the gap but also starts checking every string literal anywhere inside a JSX subtree, surfacing ~130 false positives (CSS variant/size enums, htmlFor refs, state-machine action strings, fallback display chars) across both apps that need a dedicated `callees`/`object-properties` exclude-list tuning pass to resolve properly. The specific hardcoded strings found during this review were fixed directly; the gate itself doesn't yet prevent new regressions in this category.
- No `CHECK` constraint or enum type on `users.preferred_language` [supabase/migrations/0015_users_self_service_language_preference.sql] — DB-level validation relies entirely on the app-layer Zod schema; consistent with this project's "no CHECK constraints on business-value columns yet" precedent (`gyms.default_language` has the same gap).
- `scripts/check-i18n-key-parity.mjs`'s `LOCALE_DIRS` is a hardcoded array, not a repo-wide discovery of `locales/{en,fr}.json` pairs — a future locale-bearing package (e.g. `apps/mobile`) is silently excluded from the key-parity gate until someone remembers to add it.
- ESLint's inherited `words.exclude: ["[A-Z_-]+", ...]` whitelists any all-caps-only literal, which could hide a real untranslated UI string — this is the plugin's own upstream default, not introduced by this story; changing it is a broader linting-policy call.
- `apps/super-admin/eslint.config.mjs`'s dead-code `{ ignores }` block (11 Supabase-starter scaffold files) relies on a one-time grep verification with no automated re-check if any of those files becomes reachable again.
- Locale strings paired with dynamic counts (`gracePeriodUnit`, `capacityUnit`, `alertAutoDismissUnit`) have no i18next plural (`_one`/`_other`) suffixes, so e.g. a grace period of 1 renders "1 days" — fixing properly touches multiple already-shipped component call sites; better as a dedicated pluralization pass across the whole app.
- No super-admin-scoped RLS UPDATE policy exists on `users` for phone changes [supabase/migrations/0015_users_self_service_language_preference.sql] — FR-023's "phone changes require admin intervention" has no built admin-side path yet; belongs with a future user/admin-management story.
- `client-provider.tsx`'s `locale` prop isn't re-applied via `changeLanguage` if it changes after mount — narrow edge case; the only real language-change flow (`LanguageToggle`) already calls `i18n.changeLanguage()` directly and imperatively.
- `check-i18n-key-parity.mjs` throws a raw unhandled error (stack trace) on missing/malformed locale JSON instead of a clean CI-friendly message — still fails the build correctly, just with a less polished error.

## Deferred from: code review of story-1-9-gym-branding-operational-settings, follow-up pass (2026-07-10)

- No Storage cleanup on gym deletion/deprovisioning [supabase/migrations/0014_gym_settings_owner_access.sql] — a deleted gym's public logo would remain fetchable indefinitely at its predictable `{gym_id}/logo.{ext}` path; not actionable now since no real "delete an established gym" flow exists yet (`deleteGym` in apps/super-admin/services/gyms.ts is only `createGym`'s internal provisioning-rollback, called before an owner could ever have uploaded a logo). Revisit when a real gym-deprovisioning story lands.
- `fieldErrors.defaultLanguage` is computed in `SettingsForm`'s validation but never rendered [apps/dashboard/app/(dashboard)/settings/SettingsForm.tsx] — unreachable via the UI today since the field is a fixed `<select>` with only valid enum options; low-priority consistency gap with the other fields' error display.
- Business-rule bounds (grace period 1-30, alert auto-dismiss 1-120, hex color format, positive capacity) are enforced only in the Zod schema, not via a DB CHECK constraint or the RLS `WITH CHECK` clause [packages/types/src/schemas/gym.ts] — a direct Supabase REST API call from an authenticated owner session bypasses all of them; consistent with this project's already-documented "no CHECK constraints on business-numeric columns yet" precedent.
- "Remove" on the Settings page's Logo upload zone clears the client-side preview only — it doesn't delete the underlying Storage object or persist the removal server-side [apps/dashboard/app/(dashboard)/settings/SettingsForm.tsx] — already flagged as a known scope gap in this story's own Completion Notes List; no `removeLogo` service/Server Action was ever specified.

## Deferred from: code review of story-1-9-gym-branding-operational-settings (2026-07-10)

- Any authenticated staff role (manager/receptionist/coach) can read the full Settings payload, including `gym_token` (the check-in QR secret), via direct route access to `/settings` [apps/dashboard/services/gym-settings.ts:50-56] — `getGymSettings` relies on the "read own gym" SELECT policy from `0009_auth_hook_gym_claims.sql`, which predates this story and is not owner-scoped; only the Sidebar nav link hides the route from non-owners. Writes are correctly RLS-enforced (owner-only), but this read-side exposure isn't new or widened by this story — pre-existing since Story 1.3/1.8. Revisit if `gym_token` should be owner-scoped at the RLS SELECT level, or narrowed to a dedicated owner-only view.

## Deferred from: code review of story-1-8-gym-owner-login-role-filtered-dashboard-shell (2026-07-10)

- Suspense fallback swaps to a second, disconnected `LoginForm` instance while `searchParams` resolves [apps/dashboard/app/auth/login/page.tsx] — theoretically loses typed input if the swap happens after the user starts typing; low practical likelihood since `searchParams` resolution involves no real I/O (near-instant, single server-render pass), and a proper fix (e.g. React's `use()` hook instead of a fallback swap) is disproportionate for a foundation-shell story. Revisit if real users ever report losing login-form input.

## Deferred from: code review of story-1-6-super-admin-tier-management-gym-lifecycle, patch verification pass (2026-07-09)

- Before/after audit value capture (`updateGymStatus`/`updateGymTier`/`updateGymCapOverride`) is two non-atomic PostgREST round trips (a SELECT then a separate UPDATE), not one transaction [apps/super-admin/services/gyms.ts] — under concurrent admins editing the exact same gym in the same narrow window, the recorded `previous_*` audit value could reflect a stale read; closing this properly needs a new atomic `SECURITY DEFINER` RPC (matching `platform_metrics()`/`gym_member_count()`'s precedent), a real scope increase not justified at this platform's current scale (NFR-009: 1-3 gyms). Revisit if concurrent super-admin usage becomes real.
- The no-op guard's UPDATE (`updateGymStatus`/`updateGymTier`/`updateGymCapOverride`) still executes unconditionally, before the no-op comparison, even when the value is unchanged [apps/super-admin/services/gyms.ts] — currently harmless since no `updated_at`/`updated_by` trigger exists anywhere in the schema (verified via grep across all migrations); revisit if such a trigger is ever added to `gyms`.
- `GymsPageClient`'s `search`-prop resync `useEffect` could theoretically overwrite a fast re-typed search value against a slow navigation round trip [apps/super-admin/app/(admin)/gyms/components/GymsPageClient.tsx] — narrow timing window; this app has no live/debounced search (URL only updates on explicit Enter/Search-button submit), so realistic exposure is small, and a proper fix needs more invasive state-management restructuring than the original one-line intent.

## Deferred from: code review of story-1-6-super-admin-tier-management-gym-lifecycle (2026-07-09)

- `gyms` UPDATE RLS policy (`super_admin_update_gyms`) is not column-scoped — grants Super Admin write access to every `gyms` column, not just `status`/`tier_id`/`member_cap_override` [supabase/migrations/0011_super_admin_tier_gym_lifecycle.sql] — matches this story's own documented design decision that RLS authorizes by role, not by which columns an UPDATE touches; worth narrowing in a future migration if the broader security surface becomes a concern.
- `gyms.capacity` (pre-existing nullable integer, migration `0002_gyms_and_tiers.sql`) and the new `gyms.member_cap_override` are two similarly-named cap concepts with no documented relationship — pre-existing column, untouched by and unreferenced anywhere in current app code, not caused by this story's change.
- `listGyms()` uses `count: "exact"` on every page load/search/filter/pagination call [apps/super-admin/services/gyms.ts] — performance/scale concern only, not a correctness bug, negligible at current gym counts.

## Deferred from: code review of story-1-5-super-admin-create-onboard-a-gym (2026-07-08)

- Multi-step `createGym` orchestration via 5 separate round-trips rather than one Postgres transaction [apps/super-admin/app/(admin)/gyms/actions.ts] — already the best achievable design given `auth.admin.createUser` is a GoTrue API call, not a SQL operation, so true single-transaction atomicity isn't possible regardless; the actionable mitigation (fixing the missing `gyms` DELETE RLS policy) was applied as a patch, not a redesign.
- `is_super_admin()`/JWT claims have no DB-backed revocation check (a de-provisioned Super Admin retains access until token refresh) [supabase/migrations/0010_super_admin_gym_provisioning.sql] — pre-existing characteristic of the entire claims-hook system built in Story 1.3, not introduced by this story.
- `idx_gyms_name_unique` isn't created `CONCURRENTLY` [supabase/migrations/0010_super_admin_gym_provisioning.sql] — real but inconsequential at current/pilot scale (NFR-009: 1-3 gyms), and Supabase migrations run inside a transaction where `CONCURRENTLY` typically isn't usable anyway.
- `listGyms()` has no pagination [apps/super-admin/services/gyms.ts] — a real scalability gap relative to the "hundreds of gyms" architectural target, and SA-02's own mockup shows pagination controls that weren't built this story. Natural fit for Story 1.6, which already extends the Gyms pages.
- Audit log stores the owner's phone (PII) in an immutable jsonb column with no redaction/retention policy [apps/super-admin/services/gyms.ts, `log_audit_event` metadata] — pre-existing, already-accepted tradeoff of the whole `audit_log` design from Story 1.4, not unique to this story.
- AC #3's tenant isolation was verified via manual/pgTAP testing rather than a committed automated test exercising the real `createGym` code path end-to-end — consistent with this project's documented "no automated E2E dashboard testing in V1" standard and the lack of a browser-automation tool in this environment.
- A stale/deleted `tierId` at Create Gym submit time would cause an FK violation mapped to the generic fallback error [apps/super-admin/app/(admin)/gyms/actions.ts] — not reachable today since Story 1.6 (tier deletion) hasn't shipped; revisit when it does.

## Deferred from: code review of story-1-1-monorepo-starter-initialization (2026-07-05)

- next/@supabase deps pinned to "latest" undermines --frozen-lockfile determinism [apps/dashboard/package.json, apps/super-admin/package.json] — pre-existing behavior of the official `create-next-app -e with-supabase` starter, not introduced by hand-authoring; worth pinning to real semver ranges in a later hardening pass.
- TypeScript version spread across the workspace (5.9.2 exact in root/packages/types, ^5 in apps/dashboard & apps/super-admin, ~6.0.3 in apps/mobile) [package.json, apps/dashboard/package.json, apps/mobile/package.json] — stems from using two different official starter ecosystems (Next.js vs Expo), each with its own TypeScript convention; harmonize once versions stabilize.
- packages/types has no `transpilePackages` wiring in either Next app's config (apps/dashboard/next.config.ts, apps/super-admin/next.config.ts) — currently harmless since the package only exports `export {}` (pure types, erased at compile time), but will matter the moment it gains runtime code (Zod schemas, error mapping, Supabase client factory) rather than type-only exports, which this story's own scope explicitly defers to later stories.
- No root-level tsconfig.json, only tsconfig.base.json (repo root) — common in Turborepo setups since the root has no source files to typecheck, but means opening the repo root directly in an editor has nothing to resolve against.
- .env.example bundles a single shared SENTRY_DSN variable across three apps (dashboard, super-admin, mobile) instead of per-app naming — inconsistent with the Supabase vars directly above it, which are explicitly duplicated per-framework (NEXT_PUBLIC_* vs EXPO_PUBLIC_*); its own comment says "wired in a later story," so revisit naming scheme when that story lands.

## Deferred from: code review of story-1-2-supabase-region-verification-spike (2026-07-05)

- `npx supabase projects list` failed with `LegacyPlatformAuthRequiredError` and wasn't retried with a token/login — future spikes (Notch Pay 4.1, SMS/OTP 2.1) remain dependent on manual Dashboard screenshots for region confirmation rather than a scriptable, auditable check.
- The "if region does NOT match" project-recreation/env-swap/re-verify procedure was never exercised in this story (the existing project's region happened to match) — untested path, flag for whichever future spike first hits a mismatch.
- No raw measurement artifacts (exact curl commands, raw output, Dashboard screenshot) were retained alongside the summarized figures in `docs/decisions.md` — the sole audit trail for an "irreversible" decision is paraphrased narration.

## Deferred from: code review of story 1-3-tenant-isolation-foundation-jwt-claims-hook-rls-deny-all (2026-07-06)

- No CHECK constraints on monetary/numeric columns (`tiers.monthly_price/annual_price`, `plans.price`, `payments.amount`, `gyms.grace_period_days/capacity/alert_auto_dismiss_minutes/member_cap`) — all silently accept negative values; belongs with the Epic 2/4 feature stories that populate and validate these values.
- `subscriptions` has no `CHECK (expiry_date > start_date)` and `plans.annual_discount_percent` has no bounds check (e.g. 0–100) — belongs with Epic 2/3 feature stories.
- `payments.provider_transaction_ref` is globally unique rather than gym-scoped — two unrelated gyms' manual/cash payments could collide on a placeholder reference string; payments business logic owned by Epic 4.
- `users.phone` has no `UNIQUE` constraint despite being described as the platform's primary identity mechanism (FR-001) — belongs with Epic 2's phone-OTP onboarding stories (2-1, 2-6), which will define real uniqueness/reuse semantics.
- `handle_new_user()` (the `auth.users` insert trigger) has no exception handling — unlike the login hook, a signup failure here fails loudly by aborting the `auth.users` insert; may be correct behavior but worth an explicit decision when Epic 2's onboarding flow is built.
- `job_runs.status` is nullable with no documented semantics for NULL (presumably "still running") — low severity, unused until Epic 3/4 cron jobs land.

## Deferred from: dev of story 1-4-append-only-audit-log-foundation (2026-07-08)

- ~~`supabase test db` could not be run~~ — resolved same day: Docker was found already running under WSL2 (Story 1.3's stack was already up there). Ran `supabase db reset` + `supabase test db` for real; all 46 assertions pass.
- ~~`packages/types/src/database.ts` was not regenerated~~ — resolved same day via `supabase gen types typescript --local` against the same local Docker Postgres.
- `log_audit_event()`'s caller-role boundary (the part of the "never trust caller-supplied actor" design that specifically matters) is only exercised by pgTAP calling it as `authenticated` via `set local role` + `set_config('request.jwt.claims', ...)` — not by a real end-to-end call through Supabase's actual PostgREST/RPC layer. Story 1.3's own review found two real bugs (search_path resolution, RLS-blocking-the-hook-itself) that only manifested via genuine end-to-end testing, not pgTAP alone. Worth the same manual verification pass once Epic 2/4 wires the first real `log_audit_event()` call site.
- This project's dev environment situation (Docker/Supabase CLI availability) turned out to be inconsistent across sessions/shells on the same machine — available under WSL2, unavailable from the primary shell. Worth standardizing (e.g. always working from WSL for anything `supabase`-related) so this isn't rediscovered story by story.

## Deferred from: code review of story-1-4-append-only-audit-log-foundation (2026-07-08)

- No consistency constraint between `target_entity_id`/`target_entity_type` (can be set independently, producing an ambiguous reference) [supabase/migrations/0007_audit_log.sql:26-27] — belongs with whichever feature story first populates these fields and can define the real consistency rule.
- Empty-string `action_type` bypasses the NOT NULL constraint (`''` satisfies NOT NULL) [supabase/migrations/0007_audit_log.sql:22] — low severity, matches the same class of gap Story 1.3 already deferred for other business-value columns (no CHECK constraints on monetary/business fields).
- Tests key off `action_type` string as a pseudo-unique row identifier rather than the actual returned `id` [supabase/tests/audit_log_immutable.test.sql] — a test-robustness improvement (fragile for future test additions reusing an action_type string), not a correctness bug in the current suite.
- The system-caller + gym-scoped `p_gym_id` combination is never tested (system-caller tests only use `p_gym_id = null`) [supabase/tests/audit_log_immutable.test.sql] — coverage gap only; both halves (system-caller path, gym_id passthrough) are independently tested elsewhere.

## Deferred from: code review of story-2-7-member-app-goal-experience-plan-confirmation (2026-07-18)

- Unrelated schema churn bundled into `database.ts` regeneration (`otp_resend_attempts`, `check_otp_resend_allowed`, `record_otp_resend`, `caller_has_membership`, `phone_has_membership`, `users.photo_url`, and `gym_effective_member_cap`'s return type narrowing from `number | null` to `number`) [packages/types/src/database.ts] — reflects drift from migration 0019 that had never been captured by a `gen-types` run until this story; not introduced by Story 2.7's actual schema changes. The `gym_effective_member_cap` null→non-null narrowing specifically is worth a follow-up check that no downstream code relies on it possibly being null.
