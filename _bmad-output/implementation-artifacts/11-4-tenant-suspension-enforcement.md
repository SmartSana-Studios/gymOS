---
baseline_commit: ebcab69c38ca317cd0fb1f271e9ef126682b756f
---

# Story 11.4: Tenant Suspension Enforcement

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As GymOS,
I want a gym's entire tenant suspended at the authorization layer when its SaaS subscription lapses,
so that suspension can't be bypassed by a client ignoring UI state, and access is restored the instant payment succeeds.

**Context — read this before touching any AC below. This story is a pure enforcement layer on top of machinery that already exists; it adds no new billing logic and no new table.**

**What already exists and must not be rebuilt:**
- Story 11.2 (`0071_saas_subscription_lifecycle.sql`) already flips `gyms.status` to `'suspended'` (the existing `gym_status` enum from `0002_gyms_and_tiers.sql`: `'active' | 'suspended' | 'deactivated'`) in the same `UPDATE` that sets `saas_billing_status = 'suspended'`, once `run_saas_billing_lifecycle_job()`'s grace period elapses. That migration's own comment names this explicitly: *"this is the load-bearing linkage point Story 11.4 depends on."* **Do not add a second suspension signal or a new column — `gyms.status` is it.**
- Story 11.3 (`0072_saas_billing_reminders_one_tap_pay.sql`) already reverses it: `complete_verified_saas_billing_payment()` resets `status`/`saas_billing_status` back to `'active'` and advances `saas_billing_anchor_date` the instant a Flow B payment verifies. **AC #2 (reversal) needs no new code — confirm this already works with a pgTAP assertion, don't reimplement it.**
- `private.current_gym_status()` (`0063_staff_edit_deactivation.sql`) already exists — `STABLE`, `SECURITY DEFINER`, live `select status from gyms where id = private.gym_id()`, mirrors `private.current_member_role()`'s never-throws/return-null-on-absence discipline. It has exactly one live caller today: `gym_staff_read_own_members`'s `= 'active'` clause. **This story's entire job is adding more call sites, not building the helper.**
- `initiate_saas_billing_payment()` and `update_own_owner_notification_email()` (both `0072`, both `SECURITY DEFINER`) are **the Owner's escape valve out of suspension** — `initiate_saas_billing_payment()` explicitly only rejects `status = 'deactivated'`, never `'suspended'` (read its guard clause: `if v_gym_status = 'deactivated' then raise exception`). This is deliberate, already-shipped behavior this story must not break: an Owner mid-suspension must still be able to call it. Both RPCs are `SECURITY DEFINER` and read `gyms`/`tiers`/`members` directly as their function owner — **Postgres does not enforce RLS against a table's own owner unless `FORCE ROW LEVEL SECURITY` is set** (never set anywhere in this schema), so neither RPC is affected by any RLS policy this story adds. Confirm this understanding before touching either function; do not add a `current_gym_status()` check inside them.

**The one real design problem this story must solve (read in full — this is the crux of the story):**

`private.current_gym_status()` exists but is called from exactly one RLS policy in the entire schema. NFR-018/AC #1 require *"any staff or member of that gym makes a request... denied at the RLS/auth-hook layer"* — not one policy, all of them. That means retrofitting the gate onto every gym-scoped operational table this codebase has, while leaving three things deliberately unblocked so the Owner can still see and resolve the suspension:

1. **`gyms` itself stays completely unrestricted** (no change to any existing `gyms` policy). This is not new — `"read own gym"` (`0009_auth_hook_gym_claims.sql`) has always been open to every role regardless of any other state; the Sidebar has relied on reading it post-login since Story 1.8 (`docs/decisions.md`, 2026-08-13 entry). Both apps need this read to detect and display the suspended state at all.
2. **`custom_access_token_hook()` (`0009`) is not modified.** It must keep minting a valid `gym_id`/`app_role` claim for a suspended gym's members/staff on login — if it didn't, `private.gym_id()` would return `null` for a suspended-gym Owner and `initiate_saas_billing_payment()` (which calls `private.gym_id()` internally) would break, closing the one recovery path this story must keep open. Suspension is enforced entirely at the RLS/RPC layer, never at the claims-minting layer — this is a deliberate asymmetry with the `deactivated_at`-on-`members` check the hook already does (a *deactivated member* correctly gets no claims at all; a *suspended gym*'s members still get claims, just RLS-denied on business tables).
3. **The Owner's own `initiate_saas_billing_payment()`/`update_own_owner_notification_email()` RPCs stay callable**, per the point above.

**Recommended mechanism: one new `AS RESTRICTIVE` policy per gated table, not a rewrite of every existing policy.** Postgres ANDs every `RESTRICTIVE` policy against the `OR` of all `PERMISSIVE` ones for a given command — so one `create policy "tenant_active_gate" on <table> as restrictive for all using (private.current_gym_status() = 'active' or private.is_super_admin()) with check (private.current_gym_status() = 'active' or private.is_super_admin());` per table adds the gate without touching a single existing policy's business logic (lower regression risk than editing ~15 already-shipped, already-reviewed policy bodies). **`AS RESTRICTIVE` has never been used anywhere in this schema before this story** — confirm this reading of Postgres RLS policy combination (`RESTRICTIVE` policies AND together and AND against the `OR` of `PERMISSIVE` policies) against current Postgres docs before relying on it, and treat this as a new pattern worth a short `docs/decisions.md` note, not a silent one-off. This is a deliberate, narrow exception to `ARCHITECTURE-SPINE.md`'s `AD-1` "never `FOR ALL`" convention — that rule targets differentiated per-action business policies, not a single uniform tenant-liveness gate; flag this reasoning in the migration comment rather than silently violating AD-1's letter.

**The `OR private.is_super_admin()` clause is not defensive boilerplate — omitting it breaks a real, already-shipped feature.** `members` and `payments` each carry a Super-Admin *escalated* read policy (`super_admin_escalated_read_members`/`super_admin_escalated_read_payments`, `0012_super_admin_data_access_escalation.sql`) that Super Admin's audit-logged support-escalation flow (Story 1.7, FR-072) depends on — and `audit_log` carries an unconditional `super_admin_read_audit_log`. Since `RESTRICTIVE` policies apply regardless of which `PERMISSIVE` policy would otherwise pass, a bare `current_gym_status() = 'active'` gate on these three tables would lock Super Admin out of investigating or overriding a suspended gym — the exact opposite of what a suspended gym needs (Story 11.5's Super Admin Billing view, backlog, will depend on this access continuing to work). Apply the `OR private.is_super_admin()` clause uniformly to every gated table, not just these three, so no future Super-Admin escalation policy on another table silently regresses the same way.

**Concrete table list (confirm current state at implementation time — new tables may have landed since this story was written):**

| Gate it (gym-scoped operational/business data) | Leave unrestricted |
|---|---|
| `members`, `payments`, `subscriptions`, `refunds`, `attendance_events`, `plans`, `classes`, `class_sessions`, `class_bookings`, `progress_entries`, `progress_photos`, `coach_assignments`, `member_preferences`, `session_notes`, `front_desk_alerts` — all gym-scoped (`gym_id not null references gyms(id)`, confirmed via each table's own migration) | `gyms` (point 1 above — never gate); `tiers`, `users`, `job_runs`, `payment_providers`, `messaging_provider_config` (platform-wide, not per-gym); `saas_billing_payments`, `saas_billing_notices` (Super-Admin-SELECT-only already, no gym-staff/member read policy exists to gate); `device_push_tokens` (deliberately has no `gym_id` — user-scoped, confirmed in its own migration comment) |
| `payment_discrepancies` (`gym_id` **nullable** — some rows have no gym, e.g. `missing_internal_record`) — candidate, not definitive; it's a staff/Super-Admin reconciliation surface, never member-facing, and no AC explicitly requires gating it. Confirm with the user/PO whether a suspended gym's staff should still see its own flagged discrepancies, or decide independently and record the reasoning — either answer is defensible. | — |

**The harder half of this story is not the SQL — it's that the dashboard's own login path reads `members` directly, and gating `members` breaks it unless you fix this first.** `apps/dashboard/services/session.ts`'s `getDashboardShellContext()` — the **sole** gate `app/(dashboard)/layout.tsx` relies on (its own comment: *"the single source of truth for that check"*) — does a direct client-side `.from("members").select("gym_id, role")...` query, subject to RLS via `self_read_own_membership`. Once that policy is gated, this query returns empty for a suspended gym, `shell` comes back `null`, and the layout's existing `if (!shell) redirect("/auth/login")` branch fires — **an Owner trying to reach the "pay to reactivate" screen gets bounced to the login page in a loop instead**, exactly the login-loop failure mode `DashboardLayoutData`'s own comment already warns about for a different case. This is the one regression that will look like "it works" (build succeeds, typecheck passes, the RLS gate is provably correct in isolation) but silently breaks the Owner's one recovery path in a live click-through. Read `apps/dashboard/services/session.ts` (`getDashboardShellContext()`) and `apps/dashboard/app/(dashboard)/layout.tsx` in full before writing any RLS migration.

**Required fix shape (confirm/refine at dev-story time, this is the reasoning, not prescriptive code):** before `getDashboardShellContext()`'s `members` lookup, read `gyms.status`/`saas_billing_status`/`name` for `private.gym_id()` via the still-open `"read own gym"` policy. The caller's role is already available from the JWT `app_role` claim with no DB round-trip (`session.ts`'s own existing comment: *"role -- straight from the app_role claim -- no DB round-trip needed"*). If `status === 'suspended'`, short-circuit: return a distinct shape the layout can render as one of the two new block screens below, without ever reaching the now-gated `members` query. Do not let a suspended gym fall through to the existing `redirect("/auth/login")` branch.

**Two new full-screen states, copy from `EXPERIENCE.md`'s Voice and Tone table and its "V1.5 — New State Patterns" section (verbatim, do not paraphrase — FR-132 treats the wording itself as a compliance boundary, not just style):**
- **Owner (dashboard only — `app_role === 'owner'`):** *"Your GymOS subscription payment is overdue. Pay now to restore access for your whole team."* with a one-tap pay action. Reuse Story 11.3's already-shipped `initiate_saas_billing_payment()` RPC + Pay Now button flow from `SettingsForm.tsx`'s Billing section (`openPayNowDialog()` et al.) — do not build a second Pay Now implementation; extract/reuse, or render this screen as a minimal wrapper that still mounts the existing billing component. It needs no data beyond what the suspension-check read above already fetched (`gyms` row) plus the existing RPC.
- **Everyone else (dashboard: Manager/Supervisor/Receptionist/Coach; mobile: every member) — identical neutral copy, no exceptions:** *"GymOS is temporarily unavailable for this gym. Please check back later."* **Never** mention billing, payment, subscription, or an amount owed on this screen — `EXPERIENCE.md` is explicit that Manager/Receptionist/Coach get "the same neutral treatment as members... they aren't the billing relationship either, and shouldn't see GymOS's dunning language." Do not reuse the Owner's copy or a shared generic error component for this — it is a distinct, deliberate string. [Source: EXPERIENCE.md L255-256, L2193-2198 — "Voice and Tone" table + "Gym suspended (SaaS non-payment, FR-131/FR-132)" state spec]

**Mobile app needs the equivalent gate, but its exact integration point wasn't traced during story-writing — locate it at dev-story time.** `apps/mobile/src/hooks/use-session.ts`-family (`useSession()` returns `{ session, isOnboarded, gymId, isLoading }`, referenced from `apps/mobile/src/app/_layout.tsx`) is the likely home for a `gyms.status` read gating the tab navigator, mirroring the dashboard's `getDashboardShellContext()` fix — but confirm the actual current shape of member-session resolution on mobile (it may differ from the dashboard's server-side Next.js pattern, since Expo has no server component equivalent) before assuming this hook is the right place. **Never** render the Owner-billing-aware copy on mobile — members never see it (AC #3), and mobile has no Owner-role user at all (`STAFF_ROLES` in `session.ts` explicitly excludes `'member'` from the dashboard; the inverse is also true — mobile is member-only, so this ambiguity doesn't actually arise, but don't accidentally port the Owner copy over while reusing shared components).

**Known testing gap, not this story's to fix, but it constrains what "verified" can mean here:** `deferred-work.md`'s HIGH SEVERITY entry (flagged during Story 9.4's manual-test setup, still open) — no staff role other than Owner can actually log into the dashboard today (`createStaffMember()` never sets an `email`, and the dashboard's login form is email/password-only). This means the "Manager/Receptionist/Coach see the neutral screen" behavior **cannot be manually browser-verified for those roles** with the product as it stands — only Owner (dashboard) and Member (mobile) sessions are reachable for live click-through. Verify the other roles' RLS denial via pgTAP (`set_config` / a test JWT claims override, this project's established role-simulation pattern for RLS tests) instead, and say so plainly in Completion Notes rather than silently skipping that coverage or implying it was browser-verified.

## Acceptance Criteria

1. **Given** a gym that reaches `suspended` status (Story 11.2), **when** any staff or member of that gym makes a request, **then** they are denied at the RLS/auth-hook layer (via `private.current_gym_status()`, AD-3) — not only hidden by the UI — taking effect on the next request, with no tenant data deleted or mutated (NFR-018). [Source: epics.md#Story 11.4; prd.md#FR-131, FR-132, NFR-018; ARCHITECTURE-SPINE.md#AD-3]
2. **Given** a suspended gym's successful SaaS payment, **when** it is confirmed, **then** the gym returns to `active` and access is restored immediately — reversible, symmetric with the suspension mechanism. [Source: epics.md#Story 11.4; prd.md#FR-132; EXPERIENCE.md L2198 — already implemented by Story 11.3's `complete_verified_saas_billing_payment()`, verify don't rebuild]
3. **Given** a member of a suspended gym, **when** they open the app, **then** the suspension surface never mentions billing or payment — that relationship is between GymOS and the Owner only (FR-132); the member sees a neutral "temporarily unavailable" message, not a dunning notice. [Source: epics.md#Story 11.4; prd.md#FR-132; EXPERIENCE.md L255, L2195, L2197 — this neutral treatment also applies to every non-Owner staff role on the dashboard, not members alone]

## Tasks / Subtasks

- [ ] **Task 1 — Migration: the `tenant_active_gate` RESTRICTIVE policy set (AC #1)**
  - [ ] Confirm the next-free migration number on disk at implementation time (expected `0073` — re-check for a same-numbered collision from any other in-flight story per the established coordination convention this project's `sprint-status.yaml`/`deferred-work.md` repeatedly documents).
  - [ ] Re-derive the full gym-scoped-table list from the current schema (`grep -rn "gym_id uuid not null references gyms" supabase/migrations/`) rather than trusting the table above verbatim — confirm no new gym-scoped table landed after this story was written, and confirm each table's existing policy set (does it already have a Super-Admin escalated policy that needs the `or private.is_super_admin()` clause, beyond the three named above?).
  - [ ] For each table in scope: `create policy "tenant_active_gate" on <table> as restrictive for all using (private.current_gym_status() = 'active' or private.is_super_admin()) with check (private.current_gym_status() = 'active' or private.is_super_admin());`. Write each out explicitly (matching this codebase's established one-`CREATE POLICY`-statement-per-table style everywhere else) — do not introduce a dynamic `DO $$ ... EXECUTE format(...) $$` loop; no migration in this codebase generates DDL dynamically, and a loop would be the first.
  - [ ] Decide and record `payment_discrepancies` in/out per the table above — either is acceptable, but the decision and its reasoning must be in this migration's own comment, not silently made.
  - [ ] Confirm `private.current_gym_status()`'s existing `NULL`-on-absent-`gym_id()` behavior is safe here: a `super_admin`-role session (no `gym_id` claim at all, `private.gym_id()` returns `null`) hitting one of these tables would get `current_gym_status() = NULL`, which the `= 'active'` comparison evaluates to `NULL` (falsy) — correctly falling through to the `or private.is_super_admin()` clause rather than a NULL-unsafe silent-pass, but confirm this with a pgTAP assertion (mirrors the `is distinct from`-vs-`<>` NULL-safety class already fixed twice this epic, `deferred-work.md`).

- [ ] **Task 2 — Dashboard: suspension-aware shell + two block screens (AC #1, #3)**
  - [ ] Read `apps/dashboard/services/session.ts` (`getDashboardShellContext()`) and `apps/dashboard/app/(dashboard)/layout.tsx` in full first — this task's entire risk is in not breaking the existing `mustChangePassword`/`shellError`/`redirect("/auth/login")` branches while adding a fourth.
  - [ ] Add a `gyms` status/name read (via the untouched `"read own gym"` policy) ahead of or inside `getDashboardShellContext()`'s existing `members` lookup. On `status === 'suspended'`, return a shape the layout can distinguish from "no valid session" (do not overload the existing `shell === null` case, which means something different — no staff role at all).
  - [ ] `DashboardLayoutData` (the layout): add the suspended branch before the `if (!shell) redirect(...)` check, rendering the Owner-billing screen (`app_role === 'owner'`) or the neutral screen (every other role) per the Context section's copy.
  - [ ] Owner screen: reuse Story 11.3's existing Pay Now flow (`initiate_saas_billing_payment()` RPC via `apps/dashboard/services/billing.ts`, the dialog/polling logic already in `SettingsForm.tsx`) rather than re-implementing it — extract into a shared component if `SettingsForm.tsx`'s version is too entangled with the rest of that page to reuse directly; dev's judgment on extraction boundary.
  - [ ] i18n: new keys for both screens (en/fr), reusing `EXPERIENCE.md`'s exact copy — do not hardcode English inline (this project's universal i18n discipline, enforced by `pnpm run check:i18n`).

- [ ] **Task 3 — Mobile: suspension-aware root gate + neutral block screen (AC #1, #3)**
  - [ ] Locate the actual current member-session/gym-resolution entry point (`apps/mobile/src/hooks/use-session.ts` or equivalent, referenced from `apps/mobile/src/app/_layout.tsx`) — confirm its current shape before assuming the Context section's sketch is exactly right.
  - [ ] Add the equivalent `gyms.status` check, rendering a full-screen neutral state (identical copy to the dashboard's non-Owner screen) in place of the tab navigator when suspended — mirrors `EXPERIENCE.md`'s "entire app shows a single full-screen neutral state replacing all tabs."
  - [ ] i18n: reuse the same neutral-copy keys added in Task 2 if `packages/types`/shared locale infrastructure allows it across dashboard/mobile; otherwise duplicate into `apps/mobile`'s own locale files — confirm current cross-app i18n-sharing state before assuming either way (this codebase's stated convention is per-app `services/`/locales, not shared, per `AD-7`'s "not shared across apps" rule — likely duplicate, but verify).

- [ ] **Task 4 — pgTAP coverage (AC #1, #2, #3)**
  - [ ] New `supabase/tests/tenant_suspension_enforcement.test.sql`. For a representative sample of gated tables (at minimum `members`, `payments`, `subscriptions`, `attendance_events`, `classes`/`class_bookings`, `progress_entries` — not necessarily all ~15, but enough to prove the pattern generalizes, plus every table with special handling): a `suspended`-gym staff/member session is denied SELECT/INSERT/UPDATE on each; the same session succeeds once `status` is reset to `'active'` (proves the `= 'active'` comparison, not just "policy exists"); a `super_admin` session (no `gym_id` claim) is unaffected by the gate on `members`/`payments`/`audit_log` specifically (the escalation-policy regression this story must prevent); `gyms` itself stays readable by every role regardless of status (regression guard — proves Task 1 didn't accidentally touch `gyms`'s own policies); `initiate_saas_billing_payment()`/`update_own_owner_notification_email()` still succeed for a suspended gym's Owner (the escape-valve regression guard, the single most important assertion in this file); a `deactivated` gym is also denied by the same gate (the restrictive policy's `= 'active'` check excludes both non-active states, not `suspended` alone — confirm this is the intended reading, since `deactivated` is a distinct Super-Admin action from billing suspension and epics.md's AC text only names `suspended` explicitly; flag if this needs narrowing to `<> 'suspended'` instead of `= 'active'` after confirming with the user/PO).
  - [ ] Reversal: a payment verifying via `complete_verified_saas_billing_payment()` (already-shipped, Story 11.3) flips `status` back to `'active'` and the same previously-denied session immediately succeeds on the next statement in the same test — no separate reconnection/token-refresh step, proving AD-3's "no refresh required" claim end-to-end for this specific mechanism.

- [ ] **Task 5 — Regression, live evidence, and the operational flag**
  - [ ] `supabase db reset` + full pgTAP suite; `pnpm run typecheck`; `pnpm --filter dashboard lint`/`test`; `pnpm run check:i18n` (new keys, en+fr).
  - [ ] Regenerate `packages/types/src/database.ts` if this story's migration changes any RPC signature (it shouldn't — this is RLS-policy-only — confirm the diff is empty or near-empty and say so, rather than skipping regeneration unverified).
  - [ ] **Live evidence, matching this project's established discipline:** with the user, manually flip a real (throwaway/seeded) gym's `saas_billing_status`/`status` to `suspended` directly (no need to wait a full 43-day cycle) and click through: Owner dashboard session sees the Pay Now screen and can actually complete a real reactivation via Tara Money (reusing Story 11.3's already-verified real-payment path); a Member mobile session sees the neutral screen; confirm the Owner's screen never appears for a member and vice versa. Per the Context section's testing-gap note, Manager/Receptionist/Coach dashboard sessions are not currently browser-reachable — cover those via the pgTAP suite only and say so explicitly, don't claim browser verification that didn't happen.
  - [ ] **Operational flag to raise with the user before merging (mirrors Story 11.2/11.3's own precedent for this exact kind of note):** this story makes an already-suspended gym's staff/members *actually* locked out for the first time — Story 11.2 shipped the state-machine transition three stories ago, but until this story lands, `gyms.status = 'suspended'` has had zero enforcement effect anywhere except `create_staff_member()`'s one narrow check. Once this migration deploys, any gym that has *already* silently reached `suspended` (check `select count(*) from gyms where status = 'suspended'` before merging) goes instantly, fully inaccessible the moment this ships — confirm none exist yet, or confirm the user is aware and ready for it, before merging.

## Dev Notes

- **This story adds no new table and no new billing/lifecycle logic** — it is purely an enforcement retrofit onto Stories 11.2/11.3's already-shipped state machine. If a task here starts to look like it's inventing new billing behavior, stop — that's scope creep into 11.2/11.3/11.5's territory.
- **`AS RESTRICTIVE` is new to this codebase.** Confirm the Postgres RLS combination semantics (restrictive policies AND together, AND against the OR of permissive policies) against current docs before relying on it — don't rely on training-data recall for a security-critical mechanism being introduced for the first time.
- **The `or private.is_super_admin()` clause is load-bearing, not defensive boilerplate** — omitting it on `members`/`payments`/`audit_log` breaks Story 1.7's already-shipped escalation feature. Apply it uniformly to every gated table anyway, not just these three, so a future Super-Admin policy on another table doesn't silently regress the same way.
- **The dashboard shell fix (Task 2) is the part most likely to look done when it isn't.** `pnpm typecheck`/lint/the RLS pgTAP suite can all pass while the Owner's actual recovery path is broken by a login-redirect loop — this only shows up in a real click-through (or a Vitest test of `getDashboardShellContext()`'s suspended branch specifically). Do not mark this story done without exercising that path.
- **Do not modify `custom_access_token_hook()` or `private.gym_id()`.** Suspension enforcement lives entirely in the new RLS policies and is deliberately invisible to claims-minting — a suspended gym's users must keep getting valid `gym_id`/`app_role` claims so the Owner's escape valve (`initiate_saas_billing_payment()`) keeps working.
- **Do not modify `initiate_saas_billing_payment()` or `update_own_owner_notification_email()`** to add a `current_gym_status()` check — they are `SECURITY DEFINER` and already correctly unaffected by this story's RLS changes (table owner bypasses RLS by default, no `FORCE ROW LEVEL SECURITY` anywhere in this schema); adding a check would actively break the recovery path AC #2 requires.
- **Naming/conventions** (`ARCHITECTURE-SPINE.md`'s Consistency Conventions table): the one new policy name, `tenant_active_gate`, repeated verbatim across every gated table (not a per-table-unique name) — deliberate, so `grep "tenant_active_gate" supabase/migrations/` finds every site this story touches in one shot; this is the one intentional departure from this project's usual descriptive-unique-policy-name convention, worth a one-line comment explaining why.

### Project Structure Notes

- New: `supabase/migrations/00NN_tenant_suspension_enforcement.sql` (verify number), `supabase/tests/tenant_suspension_enforcement.test.sql`, a new dashboard suspended-state screen component (exact path/name at dev's discretion, likely co-located near `DashboardChrome`/`app/(dashboard)/layout.tsx`), a new mobile equivalent screen component.
- Modified: `apps/dashboard/services/session.ts` (`getDashboardShellContext()`'s suspension branch), `apps/dashboard/app/(dashboard)/layout.tsx` (the new render branch), `apps/dashboard/locales/en.json`/`fr.json` (new suspension-screen keys), the mobile root layout/session hook (exact file TBD per Task 3), `apps/mobile`'s locale files, `docs/decisions.md` (new entry: the `AS RESTRICTIVE` pattern introduction + the live-evidence session).
- **Not modified, confirm this stays true:** `supabase/migrations/0009_auth_hook_gym_claims.sql` (the hook), `supabase/migrations/0072_saas_billing_reminders_one_tap_pay.sql`'s two Owner-escape-valve RPCs, `packages/types/src/database.ts` (no RPC signature changes expected).
