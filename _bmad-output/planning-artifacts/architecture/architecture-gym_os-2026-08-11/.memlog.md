---
scope: Update to gym_os architecture.md (existing pre-spine-format doc, 2026-07-04, V1.0 only) driven by the approved 2026-08-11 correct-course proposal adopting the V1.5 Beta-Ready PRD
purpose: Resolve the 3 design gaps gating Epic 9/11 story-writing (staff-creation RPC w/ role ceiling incl. new Supervisor role, PaymentProvider routing-context extension for SaaS Flow B, per-gym Tara Money credential storage) plus apply the still-outstanding 2026-08-08 catch-up (OtpDeliveryProvider fallback chain, WhatsAppMessageProvider), and cover the new immediate session-revocation mechanism (FR-090) and private progress-photo storage bucket (NFR-011)
altitude: project/platform spine, above epics -- same altitude as the existing architecture.md, feeds bmad-create-epics-and-stories for Epics 9-13
updated: 2026-08-11T21:05
---

- (event) Working mode: Coaching path, spine-only deliverable (user choice). Existing architecture.md (2026-07-04, pre-spine-template format, V1.0-only) is the base to distill from -- no prior memlog exists for it, so V1.0 content is carried forward as ADOPTED via direct read of the doc, not re-elicited.
- (decision) [ADOPTED, 2026-08-08 proposal] OTP delivery becomes an ordered fallback chain, not single-provider selection: EvolutionApiProvider -> TwilioWhatsAppProvider -> TwilioSmsProvider -> SentDmProvider, all implementing the existing OtpDeliveryProvider interface. Chain runner replaces send-sms-hook's OTP_PROVIDER-env-var switch; advances to next provider only on failure (network error, non-2xx, or DeliveryResult.success:false); each attempt logged. EvolutionApiProvider gated by its own sandbox spike (Story 2.9, backlog) before joining the production chain -- a spike failure leaves the existing 3-provider chain as production path. Binds: any future OTP-sending code path must call the chain runner, never a single provider directly. Prevents: a new call site re-introducing single-provider coupling.
- (decision) [ADOPTED, 2026-08-08 proposal] New WhatsAppMessageProvider interface (send(phone, message, locale) -> DeliveryResult, free text, no code/template constraint) added alongside OtpDeliveryProvider for non-OTP sends -- starting with member invitations (FR-082, Story 2.10 backlog). V1 ships one implementation, EvolutionApiMessageProvider, called from a Next.js Server Action (sendMemberInvite), following the established Deno-to-Node porting pattern (sendTempPasswordMessage.ts). No new Edge Function -- the two-Edge-Function boundary (notch-pay-webhook, send-sms-hook) is unchanged. New platform-wide messaging_provider_config table (mirrors tiers' shape: Super-Admin CRUD, audit-logged writes).
- (decision) [ADOPTED, 2026-08-11 correct-course] Per-gym Tara Money credential storage uses Supabase Vault, agreed over pgsodium/app-layer encryption -- least code to own and maintain. Binds: any per-gym secret needing encryption-at-rest follows this same mechanism rather than a new one. Rationale lives in sprint-change-proposal-2026-08-11.md Section 4.2, not restated in the spine.
- (decision) [ADOPTED, 2026-08-11 correct-course] Progress photos (NFR-011) get a new, separate Storage bucket -- private (not public=true like the existing member-photos bucket from Story 2.6), signed URLs, non-guessable paths. Per-photo coach-sharing consent per PRD FR-095: default-off, immediate-revoke, non-retroactive viewing history -- revocation must invalidate any already-issued signed URL for that photo, not just block new ones (short signed-URL TTL, re-verified per photo, is the mechanism).
- (decision) Immediate role/session revocation (FR-090, extended to FR-089 per proposal's Reviewer Gate finding): RLS stops trusting the JWT's app_role claim for authorization. New STABLE helper private.current_member_role() performs a live lookup against members (scoped by auth.uid() + private.gym_id()) on every RLS evaluation, replacing app_role-claim reads in every policy that gates on role (not just Epic 9's new tables -- an existing-policy migration). gym_id itself stays claim-derived (rarely mutates; the accepted multi-gym-membership edge case is unchanged). A role change or deactivation takes effect on the member's very next query -- no token refresh, no separate revocation-list mechanism. Binds: every RLS policy checking role (existing and future) reads private.current_member_role(), never auth.jwt() ->> 'app_role' directly. Prevents: a new policy re-introducing the stale-claim window this AD exists to close. User-chosen over short-TTL-tokens-plus-refresh-revocation (rejected: platform-wide session-lifetime cost for a staff-only problem, and still not literally immediate).
- (decision) Staff account creation (Epic 9, FR-087) follows Story 1.5/1.11's existing shape: role-ceiling check runs first inside the Owner/Supervisor's normal RLS session (via the create_staff_member RPC, see next decision), then a Server Action uses the service-role admin client to call supabase.auth.admin.createUser (Postgres functions cannot call the Auth Admin API directly -- confirmed precedent from Story 1.5). New staff activation reuses Story 1.11's existing temp-password-over-WhatsApp mechanism as-is, no new activation flow. Binds: staff creation is a Server Action + service-role step, not a pure RLS-native insert. Prevents: inventing a second, divergent activation UX for staff vs. the owner-activation path already shipped.
- (decision) Role-ceiling check (caller cannot create/edit an equal-or-above role -- FR-087 creation, FR-089 edit + self-edit, NFR-013) lives in one canonical SECURITY DEFINER RPC pair (create_staff_member, update_staff_role), internally checking caller-role-vs-target-role against a hard allowlist, mirroring log_audit_event()'s internal-authorization-check pattern rather than deriving actor/role from caller-supplied parameters. Reused by both creation and edit/self-edit call sites so the ceiling rule (Owner creates Supervisor/Manager/Receptionist/Coach; Supervisor creates Manager/Receptionist/Coach only, never Supervisor/Owner; Manager creates nothing) can't drift between them. Rejected: a plain RLS WITH CHECK comparing ranks via a private.role_rank() helper -- viable for the check alone, but the RPC is still needed to gate the admin-client step (previous decision), so centralizing the ceiling logic in that same RPC avoids a second place the rule could diverge. New member_role enum value: supervisor, inserted between owner and manager in the hierarchy.
- (decision) SaaS billing (Epic 11, Flow B: gym-to-GymOS) gets its own saas_billing_payments table, Super-Admin-scoped RLS, distinct from the gym-scoped payments table (Flow A: member-to-gym). Mirrors this codebase's own established precedent (job_runs, audit_log: platform-level/global concerns get their own table rather than a nullable gym_id bolted onto a gym-scoped one -- see docs/decisions.md audit_log Decision 2). PaymentProvider gains a discriminated routing context, {type:'gym', gym_id} | {type:'platform'}, selecting which credential set (per-gym Tara Money via Vault, vs. GymOS's own platform Tara Money account) to use at initiation, verification, and reconciliation -- but each flow's payment rows live in the table shaped for its own RLS audience and its own reconciliation/discrepancy rules (FR-036/FR-137's 4th discrepancy category -- wrong-account settlement -- applies within a flow, not across the two tables). Rejected: a unified payments table with nullable gym_id + a flow enum -- would require every existing Flow-A-only RLS policy and reconciliation query to add a flow/gym_id-null branch it doesn't otherwise need, for a table whose two flows already have fully disjoint audiences.
- (decision) Class booking capacity enforcement under concurrency (FR-105): a SECURITY DEFINER book_class_session() RPC row-locks the class_sessions row (SELECT ... FOR UPDATE) before counting existing bookings and inserting -- atomic check-then-insert in one transaction, self-checking caller role/tenant like check_in()/confirm_renewal() already do (established pattern per docs/decisions.md). Not the partial-unique-index technique used for one-open-check-in (Story 3.4) -- that technique enforces uniqueness (0 or 1), capacity enforcement is a bounded count (0..N), a different invariant needing row-lock + count, not a index constraint. Flagged as an extrapolation from precedent, not a proposal-sourced decision -- confirm during Epic 12 story-writing if a lighter mechanism suffices at pilot scale.
- (decision) Offline-safe workout completion logging (FR-110) extends the offline-queue pattern to a second domain, narrowing but not violating V1.0's 'offline scope limited to check-in queue only' decision (architecture.md Working Decisions: generic-action queueing was rejected for consistency risk on sync). Workout completion is a simple idempotent upsert keyed by client_id (mark exercise/session complete, no financial or state-machine complexity, no analogous timeout-backfill concept) -- the consistency risk the V1.0 decision guarded against (ambiguous partial-import-like conflicts) does not apply here. Mobile app's existing offline-queue infrastructure (built for check-in) is reused with a second queue-item type, not a second bespoke sync mechanism. Flagged as an extrapolation, not a proposal-sourced decision.
- (event) Coaching pass complete -- 4 elicited decisions (session revocation, staff-creation flow, ceiling-check placement, SaaS billing data model) plus 4 adopted-from-proposal decisions (OTP fallback chain, WhatsAppMessageProvider, Vault credential storage, private photo bucket) plus 2 precedent-extrapolation decisions (booking capacity concurrency, offline workout-completion queueing) logged. Proceeding to Finalize: distill ARCHITECTURE-SPINE.md from this memlog + a fresh read of the V1.0 architecture.md content (carried forward as background/ADOPTED, not re-decided).
- (constraint) Reality-check against the live codebase (brownfield ratification, not re-decision): architecture.md's 'two Edge Functions only' rule is already stale -- a 3rd, gym-qr-display, shipped in Story 8.2. Edge Function count as of this spine: 3 (payment-webhook, send-sms-hook, gym-qr-display). Payment provider is DB-row+RPC-driven runtime switching (payment_providers table, activate_payment_provider() RPC, Story 4.1 decision, docs/decisions.md) -- payment-webhook is provider-generic, not literally Notch-Pay-named; Tara Money is the current is_active=true row. 50 migrations exist to date. WhatsAppMessageProvider (adopted decision above) explicitly adds no new Edge Function, so the count stays 3 after V1.5 too -- confirming Story 1.5-era rule needs updating to 'exactly 3, isolated by concern' not 'exactly 2.'
- (event) Reviewer Gate run: lint_spine.py clean (0 findings, both passes). 4 parallel subagent reviews dispatched against ARCHITECTURE-SPINE.md -- rubric walker, version/tech verification, adversarial two-independent-units attack, reconciliation against the 3 load-bearing inputs (prd.md v1.5, both sprint-change-proposals, prior architecture.md) + this memlog. Full reviews in reviews/review-*.md. All findings triaged and fixed inline at Finalize (not deferred to a list) per Finalize step 3/4: widened AD-3 to cover SECURITY DEFINER function bodies (not just RLS policies) and named the log_audit_event() regression AD-6 would otherwise have inherited, added a CI lint-gate rule to AD-3, added gym-status live-check (NFR-018) to AD-3's scope, corrected AD-6's Story-1.5-shape claim to distinguish the authorization model (different) from the account-creation step (same), stated Manager's empty allowlist explicitly, added Flow A/B webhook-dispatch + shared-discrepancy-semantics rules to AD-14, added class-attendance-vs-floor-check-in disambiguation to AD-21, excluded class booking from AD-23's offline scope, added messaging_provider_config to AD-12, added new AD-25 (push notification dispatch, pg_net) and AD-26 (member-cap trigger backstop) carried forward from the superseded architecture.md and dropped in the first distill pass, corrected Stack table versions to match the actual repo (next@16.3.0, expo@57.0.7) plus a Supabase Vault maturity caveat, added the SMS_TEST_OTP production guardrail back as a Consistency Convention, added the Evolution API unmanaged-SLA risk note tied to AD-11/AD-12, and rewrote the OQ-14/OQ-7 Deferred entries to resolve the apparent contradiction the reconciliation review flagged (OQ-14 is resolved in the finalized PRD and confirms AD-14 rather than blocking it; OQ-7 is a real remaining account-verification blocker, unrelated to any AD). Re-ran lint_spine.py after fixes: still clean.
- (event) spine finalized
